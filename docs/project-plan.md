# 项目计划

## 当前范围

使用现有 Freenove FNK0041，完成基于三路红外循迹的低速移动小车。主线是循迹、前方遇障停车与恢复、脱线处理、手动控制、故障停车和可复现测试记录，不增加树莓派或摄像头。

## 阶段与状态

| 阶段 | 工作 | 状态 |
| --- | --- | --- |
| 开发准备 | 固定工具链、GitHub、USB 主控检查 | 完成，USB 已实际上传并验证通信 |
| 控制软件 | 状态逻辑、红外/串口控制、遥测与参数 | 已实现，离线验证通过；未上传自定义行驶固件 |
| 离线验证 | Unity、主机会话测试、传感器回放、全量编译 | 已建立自动化流程，结果见开发日志 |
| 实车基线与校准 | 舵机、车轮方向、读数、车速和停车距离 | 等待电池 |
| 课程演示与报告 | 相同路线重复测试、对比原厂、整理真实结果 | 测试模板已备；实测数据尚待采集 |

## 这一版做什么

- 单条黑线和宽缓弯道循迹，不处理交叉路口选路。
- 遇障停车等待，不主动规划绕行；恢复需持续有效回波与有效线型。
- 手动控制带过期停止，模式切换先停车。
- 无回波、读数过期、低电压和错误指令退出运行模式。
- 可以通过红外独立运行，或通过有期限的电脑串口会话调试。
- 调试数据落为 JSONL/CSV，离线场景直接测试生产 C++ 逻辑。

完整行为见[控制程序](controller.md)，到货步骤见[校准指南](calibration.md)。

## 后续扩展

标记视觉、语音和主动绕障不进入当前基础版本。旧 AutoRC 仅供参考，未移植。只有实车基线稳定、老师认可范围且时间/预算允许时，再评估摄像头与电脑端 OpenCV。

工具调研见 [tooling-research.md](tooling-research.md)。已采用 Arduino CLI、pySerial 和 Unity；Zig 用来在 Windows/Linux 编译同一份 C++ 测试代码。没有安装第三方 MCP，也没有引入不使用的调度框架。

## 课程节点

本地 2026 Fall 学习计划安排：10 月 15 日所在周提案，11 月 5 日所在周进度报告，12 月 3 日所在周展示和报告。这些是周计划，最终提交日期与时刻按 Avenue 公告确认。

组号、成员和分工还需补充。提案与报告围绕实际实现、校准数据和原厂对照展开；提交材料按老师要求整理。
