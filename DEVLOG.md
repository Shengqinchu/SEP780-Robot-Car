# 开发日志

项目：SEP 780 Arduino 4WD 小车。日期按 America/Toronto 记录。

## 记录规则

- 每次开发记录：目标、实际操作、遇到的问题、解决方法、验证证据、剩余工作。
- 区分 `已实现`、`编译通过`、`已烧录`、`硬件实测通过`，不能互相替代。
- 原厂代码、团队新增代码和 AI 辅助工作分别注明，不把原厂功能当成项目创新。
- 不记录密码、令牌、家庭地址、电话号码或支付信息；失败尝试也保留事实。

## 2026-09-13：开发环境与原厂基线准备

### 目标和当前事实

- 用户已收到小车套件；根据开箱照片确认仍未完成装配。
- 用户明确说明：电池尚未到货。不能进行整车驱动、舵机定位或续航实测。
- 用户授权完成电脑端准备、维护开发日志，并将项目程序及相关开发资料上传 GitHub。
- 当前 GitHub 账号已确认；旧 AutoRC 保留不动，新项目使用独立私有仓库。

### 实际完成的电脑端工作

- 创建独立开发目录与私有仓库 `Shengqinchu/SEP780-Robot-Car`，未修改旧 AutoRC。
- 下载 Arduino IDE 2.3.10 Windows 版本，SHA-256 与官方发布资产一致；主程序 Arduino SA 签名有效。尚未把 GUI 操作当作已验证结果。
- 安装隔离的 Arduino CLI 1.5.1、Arduino AVR Boards 1.8.6、Servo 1.2.2。
- 保存原厂提交 `a035ca380a9218d0a88aab635a58b9a3c738940f` 的 29 个示例及四个依赖包，保留来源和许可。
- 新增 `usb_check`：仅裸板串口心跳和 LED 检查，不控制电机和舵机。
- 准备一键环境安装、全量编译、串口列表、显式确认后烧录的脚本，以及装配/测试清单。
- 本机前两轮编译均为 30/30 通过；原厂逐字节导入修正后的第三轮仍为 30/30，并重新编译最终 USB 检查程序成功。

### 问题、证据和解决方法

| 问题 | 观察/证据 | 处理与结果 |
| --- | --- | --- |
| 本机没有可直接调用的 Arduino IDE/CLI | 初始命令探测无对应工具，常见安装位置未发现 IDE | 使用官方固定版本，下载校验后放入独立用户缓存目录，编译不依赖全局安装 |
| CLI 版本字段判断错误 | 首次环境脚本报缺少 `Version` 属性；实际 `version --format json` 返回 `VersionString` | 修正解析并重新执行，工具链准备成功 |
| 旧例程依赖不能随便升级 | 原厂包使用 IRremote 2.2.3、RF24 1.3.2、RGB 控制库 1.0.3；当前库索引存在更新版本 | 固定原厂库包和额外 Servo 版本，构建时核对实际使用的版本 |
| Windows 自动换行影响原厂哈希 | 本机 Git 的 `core.autocrlf=true`；普通检出及第一次 archive 导出均改变文本字节 | 改为 `git -c core.autocrlf=false archive` 导出；70 个文件逐一对比原始 Git blob 成功，并用 `.gitattributes` 禁止 vendor 的换行转换 |
| 现有串口不是已确认的 Arduino | CLI 显示 6 个 Unknown 串口；Windows 设备信息表明均为蓝牙标准串口 | 不自动挑选或烧录；等待实际 USB 主控板连接后识别新增设备 |
| 超声波示例带执行器动作 | 原厂 `02.2_Ultrasonic_Ranging` 代码会自动写入舵机角度 45/90/135 度 | 程序清单明确标为会驱动执行器，装配前不能当成无动作检查 |
| 原厂测距示例存在数值范围边界 | 距离数组为 8 位，无回波返回值设置为 1000 cm，不能完整存储 | 保持原厂不改，以其作为对照；后续自主导航单独处理超时/无回波，不能把静态发现写成已发生的实车故障 |
| 首次 GitHub CI 提示旧 Action 运行时弃用 | checkout v4 引发 Node.js 20 弃用警告，但首轮 CI 本身通过 | 核对官方当前稳定版本，改用固定提交的 checkout v7.0.1 / Node.js 24，再跑 CI |

### 验证与产出

- 原厂来源校验：70/70 文件与固定提交原始 Git blob 一致；清单见 `vendor/freenove-manifest.json`。
- 仓库检查：源码哈希、11 个命名入口、PowerShell 语法、USB 源码限制和缺少硬件确认时拒绝上传均通过。
- USB 检查程序当前编译占用 Flash 2318/32256 字节、静态 RAM 193/2048 字节；这是编译器数据，不是运行时峰值或硬件测试。
- 舵机归中例程当前编译占用 Flash 1740 字节、静态 RAM 50 字节。
- 原始编译日志只留本机，不上传其中的机器路径；[Windows 全量编译摘要](artifacts/2026-09-13-windows-compilation.json) 记录各程序 Flash、静态 RAM、使用库和警告数量。
- 编译中保留了原厂/Arduino 核心的警告；没有把“零编译错误”写成“零警告”。
- 全新本地 clone 再次通过所有仓库检查，确认 Windows 自动换行不再破坏 vendor 文件。
- [首轮 GitHub Linux CI](https://github.com/Shengqinchu/SEP780-Robot-Car/actions/runs/34784599709) 对代码提交 `53de74ed9342c62ca3b06b8611413ce2fa74c69a` 完成所有检查和 30/30 编译，任务耗时 1 分 10 秒。
- 运行时警告修正后的 [最终 GitHub Linux CI](https://github.com/Shengqinchu/SEP780-Robot-Car/actions/runs/34784847884) 对代码提交 `c7687048f769b008e924ad6b025eae786bf24ffd` 再次通过所有检查及 30/30 编译，任务耗时 1 分 9 秒，旧 Action 运行时警告已消除。
- [环境证据](artifacts/2026-09-13-environment.json) 和 [CI 摘要](artifacts/2026-09-13-github-ci.json) 已归档。实际烧录次数 0，硬件测试次数 0；不能把自动编译结果当作课程演示通过。
- 本次最终补记仅修改文档和验证 JSON，不改动已经验证的程序、依赖或构建流程。已完成本阶段非实物准备，不代表最终课程项目完成。

### 下一步

- 用户：继续机械底盘装配，舵盘最终角度固定等待实际归中；电池尚未到货。
- 接裸主控板 USB 后核对板型/串口，再执行板载 LED/串口检查及必要的烧录。
- 电池到货后完成舵机实际归中和受控的轮子架空测试，再建立循迹/避障的真实测试基线。
- 组员、分工、最终新增功能及老师认可的范围尚待确认；此阶段不宣称课程项目已经完成。

### 硬件边界

- 无电池可以准备代码；USB 检查必须由用户连接确认过的裸主控板后再执行。
- 不自动选择串口，不自动烧录，不把 USB 供电当成整车电源。
- 官方要求先上传舵机 90 度程序；真正的舵机归中还需要按教程接好电源，不能仅凭编译或烧录完成判定。

## 2026-09-13 晚：机械主体照片核对与剩余附件识别

### 用户进展与观察范围

- 用户提供两张整车总览和一张剩余附件照片，询问是否装好、剩余配件用途及最右侧模块的教程。
- 总览可见四轮、上下底板、板卡和超声波转头支架已装到车体上；据此记录为“机械主体外观已装成”，不是接线或功能验收通过。
- 底部循迹模块、部分电源插口/针脚标签在照片中不够清楚；舵机真实 90 度归中也不能从转头朝向判断。
- 用户本轮未更新电池到货状态，继续沿用其此前“电池未到”的已知信息，不擅自改成已到或已通电。

### 附件问题与说明

- 黑胶带用于循迹测试路线；白色遥控器为红外控制；USB 线用于上传程序/串口。
- 透明袋里的单排针脚小板外形对应套件蓝牙模块，用于手机 App 控车。袋子遮挡及清晰度不足，具体模块型号和针脚方向尚待近照确认。
- 已定位官方第 6 章 Bluetooth control，6.1 包含模块使用和安装，6.2/6.3 包含控车示例；补充入口到 `docs/bring-up.md`。
- 红外遥控器与蓝牙模块是两条不同的控制路径；有附件剩余不等于机械装配漏件。
- 官方说明蓝牙与上传共用串口，所以上传前需要移除模块；建议当前保持未插状态，待确认针脚后再接。

### 验证与下一步

- 依据：用户本轮照片及官方 Chapter 0/3/4/6；照片未上传 GitHub，文档仅保存技术观察。
- 本轮只更新 README、装配说明和开发日志，不改动固件、原厂快照或依赖。提交前执行仓库检查，不重复编译未改变的程序。
- 需要补充车底、主板接线近照和模块拆袋后的清晰标签，才能继续核对循迹位置、供电插口和蓝牙插法。
- 尚无新增烧录、通电或运动实测；若舵盘提前固定，实际归中时再检查并按需要调整，不能把“看起来朝前”记录为校准完成。

## 2026-09-13 晚：近照确认平台、接口与部件位置

### 新证据

- 用户补充六张模块正反面、扩展板顶视、USB 接口侧视、车头和另一侧板卡近照，并询问是否接电脑调试、是否基于 Arduino。
- 下层主控板可见 USB-C 接口及未插接的圆形 DC 口；电池盒线连接上层扩展板 DC 口，与官方供电位置一致。
- 车头下方现在能看见循迹板；扩展板可见 IR-receiver 小板和空置的 Bluetooth 六孔插座。只能确认这些部件/接口的存在和位置，未证明信号、极性、所有针脚和机械间隙完全正确。
- 小模块的天线及串口针脚标记进一步支持蓝牙串口模块识别；具体芯片/板号仍无法可靠读出，不按猜测设置型号专属参数。

### 说明与下一步

- 按官方 FNK0041 教程使用 Arduino Uno 兼容平台和 C/C++ `.ino` 程序。电脑连接的是下层主控板 USB-C，不是蓝牙模块，也不是电源圆孔。
- 电脑负责上传程序及查看串口调试信息；主程序保存在主控板，完成供电/固件配置后不必拖着电脑线运行。
- 接口识别不等于上传授权；本轮没有选择串口、安装驱动、烧录、配对蓝牙或启动任何执行器。
- 电池到货状态没有更新；下一步仍为用户确认安全的主控板 USB 接线，再识别新增端口和执行主控板检查。实际电机、舵机测试继续等待合适供电。
- 本轮只更新装配说明和日志；不上传原始个人照片，不改动固件/依赖。提交前执行仓库检查，已有编译结果不冒充新的硬件证据。

## 2026-09-13 晚：USB 已连接，先完成工具与开源项目调研

### 目标与边界

- 用户表示已接 USB，但明确要求在调试前深入检索 GitHub，比较 CLI/API/MCP/Skill 和可复用的高星项目，避免依赖 Arduino IDE 截图和点击。
- 本轮先完成研究，不烧录、不打开串口、不配对蓝牙、不测试执行器；电池未更新到货状态。
- 沿用独立私有仓库，旧 AutoRC 只读，候选 MCP/Skill 不安装到全局环境。

### 实际完成

- 两轮 22 条中英文分页查询，搜索返回 822 条记录，与种子去重后保留 807 个公开仓库元数据。五个宽泛查询达到本次前 100 项上限；没有宣称穷尽 GitHub 或读完 807 个仓库。
- 通过官方文档另补 Wokwi CLI，最终比较 45 个项目，包括现有私有 AutoRC。采集并固定每项提交、星数、归档状态、README/可取得的根许可证和哈希。
- 静态检查八个独立 MCP 的关键入口、四组 Skill；另外核对 Serial Studio 的 MCP/许可、AutoRC 的当前视觉实现和 Freenove 实际引脚/驱动。
- 完成 [工具研究报告](docs/tooling-research.md)、[检索证据与复现方法](research/README.md)、[关键源码检查](research/source-audit-2026-09-13.md)；更新项目计划和第三方来源说明。
- 推荐保留 Arduino CLI，下一阶段加入受控串口 API 与离线测试。MCP、调度/通信库、视觉是分阶段候选，不是本轮已经安装的功能。

### 新观察、问题与处理

| 事项 | 实际证据 | 处理/结论 |
| --- | --- | --- |
| 新 USB 串口 | CLI 枚举到 COM5，VID/PID 1A86:7523，仍无 matching_boards | 记录新增连接；不从通用 USB 桥标识猜 MCU，不自动重装驱动或烧录 |
| 可选字段解析 | 首次过滤 JSON 直接访问 matching_boards，在严格模式下报字段不存在 | 改为检查属性存在性；这是采集脚本问题，不是板卡故障 |
| CLI 调试能力 | 官方 CLI 有 gRPC/机器接口；本机 debug check 返回 debugging_supported=false | 区分本配置的断点调试与可用串口日志调试，MCP 不能凭空增加硬件调试能力 |
| GitHub 检索同名噪声 | MCP 搜索含 Microchip 型号，skill 搜索含 Alexa 技能；car 搜索含 OBD/卡带 | 加精确关键词和厂商文档追链，按实际用途筛选，不以原始命中数充当可用工具数 |
| 种子仓库 404 | pololu/zumo-32u4-arduino 不存在；Pololu QTR 文档指向带 -library 的正确仓库 | 保留失败记录；正确名字仅作补充来源，不伪造本次重点采集成功 |
| 只读标签不是行为保证 | niradler verify_port 虽标只读，代码实际打开 serial.Serial；其选端口还可回退到第一个设备 | 不默认采用；将开串口/复位和静态枚举分开授权 |
| build_only 的语义风险 | platformio-mcp 允许 run_tests，执行 pio test 未强制 native | 后续要限制离线环境；板上测试按可能烧录对待。这是静态发现，未实际触发 |
| 高星项目的费用/许可 | Serial Studio 当前官方二进制含 Pro，GPL 自编译版另行区分 | 不按整套永久免费推荐；基础采集优先轻量 API/CSV |
| AutoRC 并非现成 ArUco | 主线 lane_detector 为 HSV/轮廓黑线检测，PC 服务用 YOLO；Git 树无跟踪权重文件 | 可借鉴分层和视觉代码，不能保证直接运行；不执行旧网络 pickle/监听路径 |

### 验证结果

- `research/Check-Research.ps1` 通过：22 条查询、807 个初筛仓库计数一致；45 个候选的元数据唯一且固定版本；12 个浅克隆 SHA 与记录一致；研究脚本语法通过。
- 本地下载的 86 份 README/许可证逐一 SHA-256 核对通过；原始内容留 `.cache/`，提交清单中的描述限制为 240 字符。
- `tests/Check-Repository.ps1` 通过：70 个原厂文件哈希不变、11 个程序入口、脚本语法、USB 无执行器静态保护、缺少确认时拒绝上传。
- 本轮仅重新编译 `usb_check`：1/1 成功，Flash 2318/32256，静态 RAM 193/2048；本地编译摘要记录 warning_count=4，未宣称零警告。既有 30/30 与 Linux CI 仍是前一阶段结果，未冒充本轮新跑。
- 结束前再次只读枚举，COM5 仍在；没有串口收发、上传或运动测试。精简证据见 [本轮摘要](artifacts/2026-09-13-tooling-research.json)。
- 本次提交限于研究脚本/快照和说明文档；没有修改固件、vendor、构建工具链或全局 Codex 配置。提交与推送状态以对应 Git 历史为准。

### 下一步

- 研究先行阶段已完成。下一轮先确认裸板/执行器隔离及蓝牙移除，再在明确授权下用 COM5 做 usb_check 的上传与固定串口会话检查。
- 建立串口限时采集、主机测试与日志记录后，等待合适电源进行原厂硬件基线验证。
- 根据基线结果决定状态机、调度和通信库；视觉和语音不进入第一阶段关键路径。所有硬件验收仍为未完成。

## 2026-09-13 晚：落地主机串口 API 与离线验证

### 执行范围

- 用户确认“去做吧”，开始实施工具路线。已在对话中询问电池盒断开、蓝牙拔下和裸板/执行器隔离情况；尚未收到这几项现场确认，因此没有开真实串口或烧录。
- 当前完成的是非实物工具层，不是自主车固件；不引入未使用的 Unity/调度库，不安装额外 MCP 或全局 Skill。

### 实际改动

- `host/requirements.lock` 固定 pySerial 3.5 universal wheel 和官方 PyPI SHA-256；安装到项目 `.local/host-pyserial-3.5`，不修改全局 Python 包或旧 Arduino 依赖。
- `host/serial_tool.py` 提供 list、软件 loopback、限时 capture、usb-check；实物操作强制明确端口及 USB 隔离确认，不接受网络 URL，不自动选板/重试/烧录。
- 默认采集 8 秒、8192 字节，最多 60 秒、65536 字节，128 字节行缓冲；单次会话保持打开，只有识别到检查固件证据才发送一次问号。只在查询后的精确应答及两条推进心跳均到达时判断通过。
- 原始 RX/TX 和摘要落到被忽略的 `artifacts/local`；异常/中断释放串口和自己的锁。只读枚举不打开端口，不输出设备序列号。
- Python 会话与 PowerShell 上传共用端口锁；禁止并发占用，强杀后残留锁需人工核查，不自动清理或抢占其他软件。
- 加入 42 项 Python unittest、PowerShell 锁一致性/互斥检查及 CI 主机测试步骤。文档入口见 [USB 自动调试](docs/serial-automation.md)。

### 问题与解决

| 问题 | 观察 | 处理与结果 |
| --- | --- | --- |
| Windows 应用目录重定向影响 pip | 初次在 AppData 工具目录创建 venv，pip 原子替换 INSTALLER 报 WinError 17 跨盘错误 | 改用项目内 `.local` 新环境，哈希锁定安装成功；失败缓存未作为有效环境使用，未动 Arduino 工具链 |
| 中文路径输出乱码 | 初次安装错误输出按非 UTF-8 编码显示路径 | Python 调用统一加 `-X utf8`，后续日志能正确显示；原始机器日志不上传 |
| 单纯开串口可能复位 | pySerial 文档说明驱动可改变 DTR/RTS | 打开前设 inactive，但不宣称绝对无复位；保留物理隔离门槛，并检测检查期间的复位迹象 |
| 波特率和旧应答可能误导通过 | USB 固件为 115200，旧 ACK 不足以证明本次查询成功 | 固定阶段协议，忽略查询前 ACK，要求至少两次推进心跳；相关回归测试通过 |
| PowerShell 正则结尾可容纳换行 | `$` 语义不如精确字符串结束严格 | 端口校验用 `\A...\z`；COM5 加换行、COM0、loop URL 均在访问硬件前拒绝 |

### 当前验证

- Windows Python 3.12.14 + pySerial 3.5：42/42 离线测试通过；软件内存回环通过。
- Python/PowerShell 锁命名一致、同端口二次获取被拒绝；非法端口被拒绝，无实际 COM9999 访问。
- `Serial.ps1 -Action list` 实际枚举到 COM5 / USB-SERIAL CH340，VID/PID 1A86:7523；其他六项是蓝牙串口。该结果不证明 MCU/bootloader 或接线正常。
- 本轮全量无上传编译重新运行，30/30 成功；70 个 vendor 文件哈希不变，原有上传无确认拒绝测试仍通过。
- 新 GitHub CI 已配置主机测试/回环及全量编译，远端运行结果将在提交后单独补记。
- 实际串口打开次数 0、烧录次数 0、执行器测试次数 0。仍需现场隔离确认，才能进入 COM5 USB 检查。

### GitHub 复现结果

- 代码提交 `852beb0608df1a6e9ed7e6771302200cce25d8d8` 已推送到既有私有仓库。
- [本轮 GitHub Linux CI](https://github.com/Shengqinchu/SEP780-Robot-Car/actions/runs/34796061715) 成功，job 耗时 1 分 23 秒；日志核对到 pySerial 3.5 安装、42 项主机测试、锁检查、内存回环和 30/30 编译全部通过。
- [精简验证摘要](artifacts/2026-09-13-host-automation.json) 保存本机/CI 结果和验证范围，未上传原始机器路径、设备序列号、虚拟环境或串口原始日志。
- 本次补记只改文档/证据，不改已经测试的代码；实际 COM5 通信和上传仍未执行，现场隔离确认仍待回复。

## 2026-09-13 晚：首次实际 USB 上传与串口检查通过

### 条件修正与授权

- 用户再次明确没有电池，询问为什么还要拔电机/舵机。重新核对 Freenove 官方组装后上传步骤：移除蓝牙、连接主控 USB、上传后再开启车上电源；没有要求逐个拆下已组装执行器。
- 因此纠正先前过严的通用“裸板/执行器拔线”条件，改为仅主控 USB、无电池或其他外接电源、上层 POWER 关闭、蓝牙未插。组装接线可保留；这不是对扩展板各供电分路的实测结论。插拔线材仍先断 USB。
- 用户在这段说明后要求“你再试试看”，本轮执行已说明的 COM5 主控检查，不运行电机、舵机或传感器测试程序。电源开关/排线状态属于现场条件，没有将其伪称为软件已测得。
- 串口脚本主参数改为 ConfirmUsbSetup / --confirm-usb-setup，旧名字保留兼容别名；缺少确认仍拒绝操作。更新 usb_check 的供电说明与文档，固件执行逻辑不变。

### 实际操作与结果

- CLI 和 pySerial 重新枚举到 COM5 / CH340，其他串口仍为蓝牙；没有自动挑选或尝试其他端口。
- 新增参数兼容测试后本机 43/43 通过，仓库检查通过，70 个原厂文件哈希不变。
- 执行 Upload.ps1 -Program usb_check -Port COM5 -ConfirmHardwareReady：首次实际上传返回 exit code 0。编译为 Flash 2318/32256 字节、静态 RAM 193/2048 字节，未改 bootloader/驱动或原厂程序。
- 执行 usb-check：1547 ms 内收到两个推进心跳（14000、15000 ms），发送一次问号后收到精确状态应答，status=passed；没有检测到检查期间复位。
- 串口打开时程序已经在运行，READY 没有重新出现；只记录实际心跳与应答，不写成“启动横幅已观测”。
- 再执行 8 秒 capture：只读接收 8 条心跳，从 49000 到 56000 ms，逐次间隔 1000 ms；没有发查询、没有检测到复位、无丢弃行或未完成行。
- 两个串口会话均关闭，项目端口锁剩余 0；本轮没有运行任何执行器测试程序。LED 物理闪烁未由现场确认，电机/舵机/传感器和电池表现仍未验证。

### 证据和下一步

- 精简记录：[首次 USB 硬件检查](artifacts/2026-09-13-usb-hardware-check.json)，包含固件源码 SHA-256、查询/采集摘要和本地原始日志哈希；原始串口文件继续留 artifacts/local，不直接上传。
- 更新 README、USB 使用说明与验证清单，避免仍显示“未烧录/未开串口”。历史记录不改写，新结果追加在这里。
- 当前板上保留 usb_check，只运行主控心跳/查询。电池到货后先做真实舵机归中和四轮架空测试，再检查传感器，不能把这次 USB 通过当成整车已可运行。

### 本轮远端验证

- 代码和实物摘要提交 `0309b5e0146733edd65d97cfe798bf1dc788ca20` 已推送。
- [GitHub Linux CI](https://github.com/Shengqinchu/SEP780-Robot-Car/actions/runs/34797661833) 成功，job 耗时 1 分 22 秒；实际日志为 43 项测试、pySerial 软件回环和 30/30 编译通过。
- 随后的补记只更新说明/CI 证据，未改变板上已上传的固件执行逻辑，也未新增真实串口会话或执行器操作。

## 2026-09-13 深夜：完成电池前的软件阶段与双语 README

### 目标

- 用户要求继续完成不依赖电池的控制程序和测试，完整记录开发过程，并把 README 写成容易阅读的中英双版。
- 本轮不烧录、不打开物理串口、不运行电机或舵机。电池尚未到货，软件校准参数先给出初始值，实测留到到货后。

### 实现内容

- 新增 `firmware/robot_car`：独立 C++ 控制器、串口命令解析、红外按键处理与 FNK0041 硬件适配。保留全部原厂文件，作为后续实测对照。
- 循迹覆盖五种有效线型；脱线立即零 PWM，短暂丢线可恢复，持续丢线或歧义线型退出运行。遇障等待采用停车/恢复双阈值、持续清晰时间和至少三次不同采样。
- 手动模式带 350 ms DRIVE 租约；串口拥有的运行模式另有 1200 ms 连接租约。PING 不续手动运动，迟来的命令不恢复过期运动，模式/来源切换先停车。
- 无回波、读数过期、电压异常和坏指令均有对应停车路径。自定义测距使用 16 位毫米和独立有效标志；不沿用原厂 8 位距离数组。超声波 `pulseIn` 设置 25 ms 超时，不声称它完全无阻塞。
- 串口固定长度分帧，支持 LF/CRLF、应答序号、坏帧/超长帧/分帧超时处理。启动时轮子停止，超声波头会居中；该程序明确标记为会控制执行器，未用 USB-only 条件运行它。
- 新增 `Robot.ps1` / `host/robot_tool.py`：只读观察、限时循迹、手动驱动、停止；先识别项目协议，运动需要两项明确确认，会话退出尝试 STOP，保存 RX/TX、遥测和结束状态。
- 新增 JSONL 到 CSV 导出、31 检查点传感器场景回放。回放执行生产 C++ 控制器，不是重新实现一套 Python 控制算法，也不是机械/电气仿真。
- 采用 Unity 2.6.1 与 Zig 0.13.0 原生测试环境。Unity 四个源码/许可文件固定 SHA-256；Zig 官方 Windows/Linux 包固定 SHA-256；所有缓存留 `.local`，无全局安装或额外 MCP。

### README 的取舍

| 参考 | 采用的方法 | 本项目落点 |
| --- | --- | --- |
| 本地 `docs-generator` | 按任务组织、渐进展开、可执行示例 | 首页先解释用途与快速开始，细节移入指南 |
| [crafting-effective-readmes](https://github.com/softaworks/agent-toolkit/blob/3027f20f3181758385a1bb8c022d4041dfb4de84/skills/crafting-effective-readmes/SKILL.md) | 以读者和项目类型选择内容，不硬套开源营销模板 | 面向组员和课程评阅，提供功能、运行、测试与代码结构 |
| [GitHub README 指南](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-readmes) | 首页负责入门，长文档分出去，内部使用相对链接 | 中英独立文件互链；校准、协议、架构和日志分别存放 |

远程 Skill 仅作为写作参考，未安装或执行其脚本。README 移除了成段免责声明、历史环境细节和内部操作规矩；保留一个简短进度说明及必要的启动行为。源码许可和贡献来源集中在 THIRD_PARTY，现场操作条件放在校准指南，不把它们堆在项目首页。

### 问题与处理

| 问题 | 处理 | 验证 |
| --- | --- | --- |
| 现有 Build -All 只枚举 USB 自定义程序 | 改为从 programs.json 枚举全部 firmware 入口，再加入原厂示例 | 新增控制器与原厂共 31/31 编译通过 |
| 本机没有可用的原生 C++ 编译器 | 采用校验过的隔离 Zig 工具，Unity 源码固定版本 | Windows 原生控制测试通过；Linux 再由 CI 复现 |
| 初次 Zig 原生链接出现 libunwind 导出属性警告 | 测试代码无需 C++ 异常/RTTI/标准 C++ 库，使用与嵌入式代码相符的关闭选项 | 最终原生编译无这些警告，项目源文件以 -Wall -Wextra -Werror 编译 |
| 空 CRLF 最初会被解析为坏帧 | 解析尾部 CR 后再检查空帧，与单独 LF 一样忽略 | 已纳入同一空行回归测试 |
| 等 ACK 后再计时会拉长控制续发间隔 | 从发送时刻安排下一次续发，而不是从 ACK 时刻计时 | 主机限时控制测试通过 |
| 实际调参会与固定数字的算法测试耦合 | C++ 算法测试使用显式基准配置；场景回放仍使用生产配置 | 单元测试保持稳定；调参后回放预期应随配置复核 |
| GPIO、读数/帧解析和 I/O 与逻辑容易混为一谈 | 单独测试控制器、串口/IR 和假硬件适配，另保留实物测试清单 | 不以假 I/O 或编译结果声称车轮、传感器已实测 |
| README 双语容易出现链接或命令漂移 | 新增文档检查：8 份指南、相对链接、成对代码围栏、首页长度与中英命令一致性 | 检查通过 |
| Unity 原始头文件自带一处行尾空格 | 保留上游字节及哈希，仅对固定 Unity 目录关闭 Git 空白检查，项目代码仍检查 | 暂存区 diff 检查通过，来源文件不被格式化改写 |

### 本机验证

- Unity：76 项测试通过，包括 10000 次固定种子传感器输入的停车/PWM 边界检查。
- Python：78 项测试通过，涵盖原有 43 项 USB 工具测试，以及项目协议、限时控制、错误清理、CSV 和场景校验；串口均为假连接或内存回环。
- 场景回放：31/31 检查点与预期状态、PWM 和命令接受结果一致。代码、输入及输出均可复现。
- 仓库检查：70 个 Freenove 原文件、4 个 Unity 文件的 SHA-256 均一致，12 个命名入口及上传/运动确认门槛通过。
- 文档检查：8 份指南、46 个相对链接、两份 README 的命令一致性通过。
- Arduino 全量编译：31/31 通过。自定义 robot_car 为 Flash 14616/32256 字节、静态 RAM 662/2048 字节；非运行时峰值内存。编译器保留旧 IRremote/AVR 核心警告，没有修改上游来隐藏它们。
- 本轮实际烧录 0 次、物理串口打开 0 次、执行器测试 0 次。到货后的实测顺序、中英校准指南和测试记录模板已经备好。

### 产出与下一步

- [中文 README](README.md)、[English README](README.en.md)、[中文校准](docs/calibration.md)、[English calibration](docs/calibration.en.md)、[控制逻辑](docs/controller.md)、[协议](docs/control-protocol.md)。
- [软件验证摘要](artifacts/2026-09-13-control-software.json)、[全量编译摘要](artifacts/2026-09-13-control-compilation.json)、[场景回放输出](artifacts/2026-09-13-line-course-replay.csv)。后者是合成场景结果，不是实车行驶记录。
- 电池到货后：先原厂基线，再校准电压/方向/黑白读数/车速/距离，最后重复路线对比。首次正式运行自定义固件仍需要现场确认与显式上传。
- 代码、文档和本机证据先提交，GitHub CI 的实际结果另行补记。

### GitHub 验证与落档完成

- 实现提交：`e7ed7dfb56d86f3e74acdb5b679eed53d8d986ed`，已推送到原有私有仓库 main。
- [GitHub Linux CI](https://github.com/Shengqinchu/SEP780-Robot-Car/actions/runs/34801602253) 成功，耗时 1 分 39 秒。原始日志再次核对到 76 项 Unity 测试、78 项 Python 测试、31 个回放检查点、文档检查、内存回环及 31/31 编译通过。
- GitHub 中文/英文 README 的实际 HTML 渲染已检查：标题、语言切换、快速开始代码和校准链接均存在；本地相对链接与两版命令也通过自动检查。
- 更新软件摘要的 CI 字段并落档。本次收尾只追加日志与证据，不改动已经验证的代码，不增加任何物理串口或烧录操作。

## 2026-09-19：电池到货，准备首次带电校准

### 准备与检查

- 用户报告电池已到，并确认完成首轮准备：两节 Samsung 30Q 18650 外皮完好、已用配套充电器充好；车上两个电源开关关闭、暂不装电池、断电移除蓝牙模块、四轮架空、转头周围无遮挡，接下层主控 USB-C。以上为用户现场确认，未由软件测量电芯电压。
- 最初电脑只枚举到蓝牙串口；用户连接后，`scripts/List-Boards.ps1` 检测到 USB 串口 COM5。Arduino CLI 将板型显示为 Unknown，未把端口出现等同于固件或执行器工作正常。
- 已请求用户明确确认以 COM5 上传原厂 `servo_center`；等待该确认期间未打开物理串口、未烧录、未驱动执行器。
- 本轮顺序：先舵机归中与机械朝向，再四轮架空检查、传感器读数，最后项目固件与低速地面测试。每阶段分别核验实际现象。

### 软件预检

- `scripts/Build.ps1 -Program servo_center`：1/1 编译通过，Flash 1740/32256 字节，静态 RAM 50/2048 字节，编译摘要记录 4 条警告。使用固定的 Arduino AVR 1.8.6、Servo 1.2.2；未升级依赖。
- 本地编译摘要：`artifacts/local/compile-20260919T214958885Z.json`，不上传含本机路径的原始日志。
- `tests/Check-Repository.ps1`：通过原厂文件与 Unity 哈希、12 个入口、PowerShell 语法及上传/运动确认门槛检查。此检查不访问真实硬件。
- 既有日志中最后确认烧录的是 `usb_check`。本轮尚未读取当前板上固件，不将历史记录当作当前固件确认。

### 上传前实测边界

- 此阶段仅完成连接枚举与软件预检；舵机、四轮、测距、循迹、电池电压及障碍停车行为均未在本轮实测。
- 后续将按实际上传结果和用户现场观察追加记录，不提前填写成功结果。

### 首次上传舵机归中程序

- 用户明确回复“确认，使用 COM5 上传”，并确认无电池、蓝牙移除、车轮架空、转头无遮挡的准备状态。
- 再次运行 `scripts/List-Boards.ps1`，COM5 仍为 USB 串口；未尝试其他端口。
- 执行 `scripts/Upload.ps1 -Program servo_center -Port COM5 -ConfirmHardwareReady`，编译和上传进程正常结束，退出码 0，工具报告 `Upload completed`。此次覆盖此前的 `usb_check`，没有上传车轮运动程序。
- 上传前编译摘要：`artifacts/local/compile-20260919T215446955Z.json`；Flash 1740/32256 字节、静态 RAM 50/2048 字节。
- 已向用户提供首次电池上电观察步骤：拔 USB、关闭两个开关后按极性装电池，供电接上层扩展板；架空车轮、避让转头，上电观察约 5 秒后关电。持续抖动、卡住嗡响、发热或非预期车轮运动时立即断电。
- 当前确认的是上传成功，尚未收到用户对舵机实际动作、角度及稳定性的反馈；不据此判定归中或整车功能通过。上传进程已经退出，未开启串口监视会话。

### 归中无可见动作：增加单轮小幅动作诊断

- 用户上电后反馈“上电成功 无反应”，并提供照片。照片可见上下板指示灯点亮，但不足以确认舵机供电电压、完整接线或转头角度；不将亮灯等同于整车功能正常。
- 核对实际已上传源码：原厂 `servo_center` 只在 `setup()` 中向 D2 舵机发送一次 90 度命令，随后空等，不控制车轮。如果实际位置已经接近中位，可能没有明显运动；这仍是假设，不是故障排除结论。
- 新增 `firmware/servo_check/servo_check.ino`，沿用固定原厂 D2 舵机与 D5/D6 电机 PWM 映射；两个电机 PWM 引脚保持低电平。每次上电/复位只执行一轮 `90 -> 80 -> 90 -> 100 -> 90` 度指令，每步 1.5 秒，然后保持中位。LED/串口用于标记程序步骤，没有角度反馈传感器。
- 原厂文件不变；在 `programs.json` 登记为会控制执行器，补充上传检查、双语校准指南与 `THIRD_PARTY.md` 来源说明。没有更改主控业务逻辑或依赖版本。
- `Build.ps1 -Program servo_check`：通过，Flash 3366/32256 字节、静态 RAM 233/2048 字节。`Check-Repository.ps1` 通过 70 个原厂文件及固定 Unity 哈希、13 个程序入口和确认门槛；这些是软件检查，不是实车测试。
- 用户再次明确确认：已关闭开关、断开电池圆形供电插头，连接下层 USB-C，蓝牙移除、车轮架空、转头无遮挡，允许使用 COM5 上传舵机诊断程序。重新枚举 COM5 后执行 `Upload.ps1 -Program servo_check -Port COM5 -ConfirmHardwareReady`，进程退出码 0，工具报告上传完成。
- 上传前编译摘要为 `artifacts/local/compile-20260919T220306445Z.json`。当前板上最近一次成功上传的是 `servo_check`，不是原厂归中程序或项目行驶程序。
- 已要求用户拔 USB、在两个开关关闭时接回上层板电池插头，架空并留空转头后上电观察约 8 秒再关电。等待实际摆动反馈；本轮未执行电机、测距、循迹或地面测试。

### 小幅动作仍无反应：暂停运动，分开排查执行与接线

- 用户再次反馈“依旧没有任何反应”。将本次舵机检查记录为未观察到动作、未通过实物验收，不能再仅用“原本在中位”解释；没有将其判定为舵机损坏。
- 复核原厂 `00.0_Servo_90` 与项目 `servo_check` 的舵机信号脚均为 D2；官方装配说明要求舵机信号线接 SIG、红线接 5V、棕线接 GND，电池供电接上层扩展板。原有照片无法清楚验证每个接点和两板排针接触。
- 已要求停止反复上电，关两个开关并拔 USB；请求断电后的 SERVO 三针接头近照及两板之间的侧面排针近照，暂不盲目拔换线材。
- 下一项电脑侧诊断为读取当前程序的串口步骤日志，区分“程序是否执行”与“物理舵机是否动作”。已请求用户确认断开电池圆形供电插头、只接 USB、蓝牙移除及 COM5；确认前不打开串口，不再次烧录，也不扩大动作范围。
- 串口开关可能复位并重跑当前程序；即使收到 `COMMAND_DEGREES` 或 `DONE`，也只能确认代码路径，不证明 D2 波形、舵机供电或真实角度。供电电压和接线接触仍待核实，电机测试继续暂停。

### USB 串口已确认完整执行，继续核查舵机接触与供电

- 用户补充上层 SERVO 接口及两板侧面照片。照片显示电池接上层板；三色线顺序看起来与 SIG/5V/GND 对应，未发现明确反插证据，但不能凭照片确认金属触点导通。照片拍摄时板灯仍亮，已提醒不得带电插拔。
- 用户随后明确确认已关开关、断开电池圆形供电插头、只接 USB，并允许读取 COM5。再次枚举到 COM5 后，复用 `host/serial_tool.py` 的端口锁和有界采集，以一次 DTR 复位捕捉启动过程；没有更换固件或发送运动串口命令。
- 实际采集约 10.047 秒、211 字节，获得 `SERVO_CHECK` 标记、依次 `COMMAND_DEGREES=90/80/90/100/90`，以及 `DONE: holding 90 degrees`；完整步骤匹配。原始记录保存在 `artifacts/local/servo-diagnostic-20260919T222403210210Z.jsonl`。
- 采集进程退出码 0，连接在 `finally` 中关闭、端口锁释放。结果仅确认 USB 供电下的固件执行路径，不证明电池供电时的执行、D2 实际波形、舵机口 5V 或物理运动，`physical_motion_verified` 仍为 false。
- 对照官方 Chapter 0 接线图后，建议先完全断电，只重新插紧 SERVO 三针接头，检查弯针/错位：信号线接 SIG[2]、红线接 5V、棕线接 GND；握插头塑料，不拽线，不更改其他接线。看不清时先拍空座，不猜测插接方向。
- 请求用户在接头确认后架空复测现有 7.5 秒单轮程序，并询问是否有万用表，以便必要时进一步区分供电与信号/舵机问题。尚未收到重插后的结果，未宣称硬件已修复，也没有进入车轮运动测试。

### 已定位并修复 SIG 漏接，转入机械朝向校正

- 用户确认“最左边的引脚是悬空的”，重新接好并上电后舵机转动；提供的接口局部图可对应到最左侧 `SIG[2]` 信号针。根因记录为舵机三针接头未完整接入、信号针漏接，而不是程序未上传或已证实的舵机损坏。
- 修复后动作由用户现场反馈确认。本轮没有重新烧录、换依赖或扩大运动范围；此前串口中的完整步骤日志与此次实物恢复动作是两项不同证据。
- 进一步询问结束位置，用户回复“能转动，但停下时偏向一边”。因此仅将“接头修正后恢复响应”标为完成，不把完整机械归中、角度精度、无抖动或整车验收记为通过。
- 当前 `servo_check` 最终命令仍为 90 度。偏斜优先按舵盘与输出轴的安装角度检查，暂不改软件偏置；请求完全断电后的舵机/支架连接近照，确认中心舵盘固定螺丝后再指导调整，不强扳舵机或拆外壳。
- 更新中英文校准指南：检查颜色顺序之外，逐根核对三针实际进入插孔，特别留意外侧 SIG 针。双语 README 同步为“舵机恢复动作、朝向校正中，车轮及传感器待测”。
- 下一步：确认转头在中位时朝正前且无卡滞，再经现场准备与端口确认进行架空车轮测试。此时尚未测试电机、测距、循迹、电池电压或地面行驶。

### 识别舵盘中心固定点

- 用户补充五张转头正面和俯视照片。与官方 Chapter 0 的 `Chapter0_34.png`、`Chapter0_35.png` 对照：左右两颗较长螺丝将超声波转接小板固定到白色舵盘，中央位置使用舵机配件袋中最小的螺丝固定到输出轴。
- 第 4、5 张照片中两颗银色螺丝之间的白色圆孔对应中央固定点；图片未清楚显示十字螺丝头，存在漏装可能，但没有将其写成已确认缺件。已请求用户在完全断电下近看确认，不误拆侧面两颗或舵机外壳，不拿较长螺丝替代。
- 斜拍照片不用于精确推断机械偏角；当前未实施拆装、重新对齿或软件角度补偿，也未再开串口、烧录或运行硬件。中心紧固情况与最终机械归中仍待现场确认。

### 用户确认中心固定螺丝漏装

- 用户现场确认“中央是空孔，还没有装螺丝”，将此前图片中的漏装可能更新为用户已确认的装配缺项。缺少中心螺丝会影响舵盘固定，但尚不能据此认定它是停止朝向偏斜的唯一原因。
- 再次核对官方 Chapter 0 中心固定图及当前 `servo_check` 源码：使用舵机原配袋中最小的固定螺丝；现有诊断序列最终保持 90 度命令，无需为机械重新对齿更换固件。左右两颗较长螺丝不属于中心轴固定螺丝。
- 下一步指导为：完全断电后轻提舵盘与转头组件脱离输出轴，妥善支撑并留足线缆余量；车轮架空、蓝牙移除、USB 断开，仅接电池让现有程序完成一轮归中，然后再次完全断电，将转头朝前重新对齿装回并用原配中心短螺丝固定。遇到拔不动、线缆拉紧或螺丝规格不明时停止，不强扭输出轴、不撬板、不使用长螺丝替代。
- 本轮仅更新操作指导与日志，没有串口访问、烧录或自动驱动。尚未收到重新装配后的结果，机械归中、紧固及复测保持待确认；车轮与传感器测试继续暂停。

### 齿位无法精确对中，准备小幅软件微调

- 用户在舵机完全停止并断电后尝试重新对齿，确认白色舵盘为离散花键：相邻两个可装位置分别偏向两侧，没有机械上正中的齿位；当前选择仍使转头相对车头顺时针偏转。用户要求改程序补偿中点并留下说明。
- 核对原厂避障程序的方向定义：`150` 度对应车头左侧、`30` 度对应右侧，因此从车顶俯视时增大命令角度可作逆时针修正。原厂 RF/蓝牙校准路径最终也将保存的 `servoOffset` 约束为 `-10..+10` 度。
- 将自定义 `servo_check` 改为显式的 `90 + kServoTrimDegrees` 中点，首轮候选微调为 `+8` 度（中点 `98` 度），诊断摆幅从中点两侧各 `10` 度缩小为各 `5` 度。源码注释写明方向、机械齿位前提及“实测后才同步正式控制器”；串口结束信息改为打印实际候选中点。
- `firmware/robot_car/config.h` 暂时仍保持 `servo_center_degrees = 90`，不把尚未实车确认的候选值写入正式控制器。补充中英文校准说明和仓库静态检查，将诊断偏置与摆幅都限制在不超过 `10` 度。
- 本阶段只修改和离线验证代码；尚未上传新的 `servo_check`，也未让舵机按 `98` 度动作。中央固定螺丝仍需安装，最终中点与正式程序同步继续等待实车反馈。
- 离线验证完成：`Build.ps1 -Program servo_check` 为 1/1 通过，Flash 3468/32256 字节、静态 RAM 233/2048 字节，摘要保存为 `artifacts/local/compile-20260919T224936843Z.json`；`Check-Repository.ps1` 通过 70 个原厂文件哈希、13 个程序入口、上传确认门槛及新增的偏置/摆幅边界；`Test-Docs.ps1` 通过 8 份指南和 48 个本地链接；`git diff --check` 无错误。这些结果只证明源码与构建检查通过，不证明 98 度实际朝前。
- 用户随后明确回复“已准备好”，沿用紧邻该回复的现场条件：中央舵盘已用原配小螺丝固定、四轮架空、蓝牙移除、电池供电断开、只接下层 USB，并确认使用 COM5。重新枚举时 COM5 仍是列表中唯一标记为 USB 的串口。
- 执行 `Upload.ps1 -Program servo_check -Port COM5 -ConfirmHardwareReady`，编译与上传退出码均为 0，工具报告 `Upload completed`；上传时 Flash 3468/32256 字节、静态 RAM 233/2048 字节，摘要为 `artifacts/local/compile-20260919T225404120Z.json`。当前板上最近一次成功上传的是候选中点 98 度、摆幅正负 5 度的 `servo_check`。
- 本次上传未证明舵机实际到达 98 度或超声波头朝前。等待用户拔掉 USB、在开关关闭时接回上层板电池供电，并在四轮架空、转头无遮挡条件下完成一次约 8 秒观察后关电；正式控制器仍未更新或上传。

### 98 度实测扩大偏差，反向调整候选中点

- 用户完成电池供电测试并提供最终停留状态俯视照片，反馈偏移仍然较大。照片显示超声波转头相对车身纵向明显偏斜；单张带透视照片不用于宣称精确机械角度，但足以判定 98 度不是可接受中点。
- 首轮从 90 度增加到 98 度后偏差扩大，说明先前依据原厂扫描数组推断的补偿方向不适用于这台实车当前装配方向。源码注释改为以本车物理反馈为准，不把通用方向推断继续当成事实。
- 将 `servo_check` 候选偏置从 `+8` 改为 `-8`，候选中点由 98 度改为 82 度；5 度单侧摆幅不变，因此下一轮序列为 `82 -> 77 -> 82 -> 87 -> 82`。偏置仍在原厂约正负 10 度的校准范围内。
- 本轮修改时用户照片显示小车仍处于电池带电状态，因此只修改并准备离线验证，不自动访问 COM5 或上传。正式 `robot_car` 中点仍保持 90 度，等待 82 度实车结果。
- 第二轮候选程序离线验证完成：`Build.ps1 -Program servo_check` 为 1/1 通过，Flash 3468/32256 字节、静态 RAM 233/2048 字节，摘要为 `artifacts/local/compile-20260919T225841010Z.json`；仓库检查确认偏置 `-8` 度和摆幅正负 `5` 度均在边界内，70 个原厂文件哈希与 13 个入口通过；文档检查和 `git diff --check` 也通过。没有在本步骤上传或驱动硬件。
- 用户再次明确回复“已准备好”，确认电池供电断开、只接下层 USB-C、中央螺丝固定、四轮架空、蓝牙移除且转头无遮挡。重新枚举后 COM5 仍是唯一标记为 USB 的串口。
- 执行 `Upload.ps1 -Program servo_check -Port COM5 -ConfirmHardwareReady`，编译和上传退出码为 0；Flash 3468/32256 字节、静态 RAM 233/2048 字节，摘要为 `artifacts/local/compile-20260919T230148275Z.json`。当前板上最近一次成功上传的是候选中点 82 度、摆幅正负 5 度的诊断程序；等待电池供电下的最终朝向反馈。

### 接受 82 度视觉中点并同步正式控制器

- 用户完成 82 度电池供电测试并提供与 98 度状态的两张俯视照片，反馈“有点区别，但不大”。对照车身中线与超声波横梁，82 度状态比 98 度明显更接近正前；剩余轻微视觉斜角无法从不同拍摄透视中可靠量化。
- 当前决策是不继续依据照片反复进行 1 至 2 度烧录，而将 82 度接受为项目的初步实车中点。后续以正前方平面目标的超声波读数和障碍停车表现作功能验收；传感器测试仍未执行，不能把视觉归中写成完整避障通过。
- 将 `firmware/robot_car/config.h` 的 `hardware::servo_center_degrees` 从 90 改为 82，并补充机械齿位加软件微调的注释。仓库检查新增一致性约束，要求诊断程序的 `90 + trim` 与正式控制器中点相同，避免后续两处漂移。
- 同步更新中英文校准指南与 README 进度。当前板上仍是 `servo_check`；正式 `robot_car` 仅修改源码，尚未重新编译、上传或进行车轮/传感器实测。
- 同步后离线验证通过：`Build.ps1 -Program robot_car` 为 1/1 成功，Flash 14616/32256 字节、静态 RAM 662/2048 字节，摘要为 `artifacts/local/compile-20260919T230506566Z.json`；76 项原生控制测试零失败；仓库检查确认正式中点 82 与诊断偏置一致，原厂文件哈希及上传门槛不变；双语文档检查和 `git diff --check` 通过。上述结果不代表正式固件已经上传，也不代表超声波、车轮或避障实测通过。

### 转入最终固件并合并障碍蜂鸣提示

- 用户询问是否需要继续大量测试以及何时进入最终项目。调整执行策略：不逐个重复全部原厂示例，改为三道综合门槛，即正式固件架空上电/状态读取、每个方向最多约 1 秒的架空短动、地面低速循迹加障碍。完整 HW-01 至 HW-15 保留用于最终报告和失败定位，不作为开始正式固件前的串行前置任务。
- 在首次上传正式固件前合并用户已确定的障碍蜂鸣需求，避免先后两次刷写。循迹模式下，前方有效距离仍处于 200 mm 停止阈值内时，板载有源蜂鸣器每 700 ms 短鸣 100 ms；移开障碍或退出循迹模式后立即静音。实现不使用 `delay()`，不改变控制器的停车、迟滞恢复和故障规则。
- FNK0041 的 A0 同时用于电池分压和有源蜂鸣器。适配层仅在鸣叫窗口将 A0 切为输出高电平，静音时恢复输入；鸣叫期间保留最近一次电池值，静音窗口继续采样。新增编译期约束，要求鸣叫时间大于零且短于周期。
- 新增两项假 I/O 测试，覆盖鸣叫脉冲/A0 恢复/电池值保持，以及关闭告警后立即静音。`tests/Test-Control.ps1` 实际运行 78 项、0 失败；`Build.ps1 -Program robot_car` 为 1/1 成功，Flash 14824/32256 字节、静态 RAM 668/2048 字节，摘要为 `artifacts/local/compile-20260919T231242858Z.json`。
- `tests/Check-Repository.ps1` 通过 70 个原厂文件哈希、固定 Unity 哈希、13 个程序入口、脚本语法与上传/运动确认门槛；`tests/Test-Docs.ps1` 通过 8 份指南和 48 个本地链接；`git diff --check` 通过。所有结果均为离线/静态验证。
- 当前板上最近一次成功上传的仍是 82 度中点、正负 5 度摆幅的 `servo_check`。本阶段没有打开 COM5、没有上传 `robot_car`、没有驱动车轮，也没有验证蜂鸣器真实发声。手机语音控制仅列为下一项安卓端集成，尚未实现或实测。

### 首次上传正式 robot_car 固件

- 用户明确确认上传条件：“已准备好，确认 COM5 上传正式固件”；该确认紧邻的准备要求为两个开关关闭、上层板电池圆形供电插头断开、蓝牙模块移除、四轮架空、转头无遮挡、只接下层 USB-C。
- 上传前重新运行 `scripts/List-Boards.ps1`，COM5 仍是列表中唯一标记为 `Serial Port (USB)` 的端口；没有选择其他蓝牙/普通串口。
- 执行 `scripts/Upload.ps1 -Program robot_car -Port COM5 -ConfirmHardwareReady`。编译与上传进程退出码为 0，工具报告 `Upload completed`；Flash 14824/32256 字节、静态 RAM 668/2048 字节，编译摘要为 `artifacts/local/compile-20260919T231700376Z.json`。
- 当前主控最近一次成功上传的程序更新为正式 `robot_car`，覆盖此前的 `servo_check`。上传过程未发送 ARM、DRIVE 或循迹启动命令；程序设计为启动时轮子零输出并等待显式启动。
- 此结果只证明编译及传输成功。尚未接入电池执行正式固件的启动观察，未读取真实遥测，未验证轮子方向、循迹、测距、蜂鸣声或障碍停车/恢复，也未把这些项目标记为通过。

### 正式固件 USB-only 启动观察

- 沿用用户已确认的电池圆形供电断开、蓝牙移除、四轮架空、转头无遮挡和 COM5 条件，执行一次 5 秒 `Robot.ps1 -Action observe`。该动作不发送 ARM、DRIVE 或 STOP；打开串口可能复位并重新归中，用户已为转头留出空间。
- 会话正常完成并收到 26 条正式 v1 遥测，原始本机记录为 `artifacts/local/robot-20260919T231840Z-4ba749b0.jsonl`。结构化复核确认全部记录均为 `mode=idle`、`state=idle`、`line=7`、左右 PWM 均为 0；非零 PWM 记录数为 0。
- USB-only 条件下，超声波报告范围为 331 至 378 mm；这证明本次串口会话收到了变化的测距字段，不等同于经过直尺校准或不同目标验证。电池字段始终为 547 mV，符合未接电池时的低读数，只用于确认当前供电条件，不能作为电池电压校准。
- 该结果把 HW-11 更新为“USB-only 启动保持 idle/零 PWM”；电池供电启动、真实舵机位置、车轮方向、传感器极性、蜂鸣和地面功能仍未通过实测。

### 正式固件电池启动通过

- 按要求关闭两个开关、拔掉 USB、接回上层扩展板电池圆形插头，在四轮架空和转头无遮挡条件下开机约 5 秒再关机。用户反馈“正常”，对应本步骤约定的正常现象为转头归中、四轮不动、蜂鸣器不响；据此将正式固件电池启动和默认安全输出记为用户现场观察通过。
- 此处只验收启动表现，不推断具体电池电压、测距精度、循迹极性、电机方向或障碍蜂鸣。下一道门槛改用电池供电加红外遥控的架空短动，避免为方向测试额外引入 USB 线缆。

### 首次电池加 USB 遥测未通过传感器门槛

- 复核 Freenove 官方 `01.2.2_Battery_level` 流程：上传后明确要求保持 USB 连接，再开启小车开关并通过 Serial Monitor 读取电池，因此本次按官方允许的 USB 加电池方式观察，不发送运动命令。来源：<https://docs.freenove.com/projects/fnk0041/en/latest/fnk0041/codes/tutorial/1_Control_Basic_Components.html>。
- 用户确认电池插头接在上层板、蓝牙移除、四轮架空、转头无遮挡、USB 接下层板，并在车头约 40 至 60 cm 放置平整目标后回复“带电观察已准备好”。重新枚举确认 COM5 仍是唯一 USB 串口，执行 10 秒只读 `Robot.ps1 -Action observe`。
- 会话正常完成，收到 51 条遥测且非零 PWM 记录为 0，全部保持 `mode=idle`、`state=idle`、`line=7`。本机原始记录为 `artifacts/local/robot-20260919T232437Z-2577fc6b.jsonl`。
- 电池字段从启动时 195、449、508 mV 上升，随后只在 527 与 547 mV 附近，整段范围 195 至 547 mV；这与两节 18650 的实际供电不符，也与 USB-only 最终 547 mV 相同。不能据此校准比例或放宽 6600 mV 低压门槛，先检查两个电源开关和电池检测路径。
- 超声波前两条无有效回波，之后 49 条稳定在 85 至 87 mm，而现场准备目标声明为约 400 至 600 mm。该读数可能来自近处遮挡或目标距离/位置不符，尚不能据此判定传感器坏，也不能把测距标为通过。
- 因电池和距离两项均未达到启动条件，停止进入运动测试。下一步只复核供电是否在拔掉 USB 后仍保持板灯、以及从两个超声波金属探头正面量起移除 20 cm 内物体并重新摆放目标；不改控制参数掩盖未确认的硬件条件。

### 断开 USB 后板灯熄灭，更正电池启动结论

- 用户按要求在四轮架空且未发送运动命令时只拔掉 USB-C，反馈“拔 USB 后灯灭了”。这直接证明刚才所谓带电遥测实际仍由 USB 供电，电池路径当时没有为整车供电；因此 195 至 547 mV 不能解释为真实电池电压。
- 此证据也使此前仅凭“正常”反馈记录的正式固件电池启动通过失去成立条件：当时没有独立确认拔掉 USB 后板灯保持点亮。开发日志保留原始反馈，但验证清单更正为电池启动待重测，不用后续证据反向美化早期结论。
- 先排查官方明确要求的两个开关，即电池盒开关和上层扩展板 POWER 开关，以及电池极性、触点和上层板圆形供电插头。电池供电独立点亮之前，不接入运动测试，不放宽低压保护，也不再次采集所谓带电遥测。

### 更正开关状态后确认电池独立供电

- 用户保持 USB 断开，确认圆形插头位于上层扩展板，依次开启电池盒开关与上层板蓝色 POWER 开关后反馈“正常亮”。这确认当前电池链路能够在没有 USB 时点亮车板，前一次断电源于当时未实际开启电池路径，而不是已经证实的电池、插头或扩展板故障。
- 该观察只确认无 USB 时板灯供电，不替代电池电压遥测、带负载稳定性或具体执行器验收。下一步在两个开关关闭时重新接 USB，按官方电池监视流程开启两个开关并复测；运动继续保持禁用，直至电压与前方距离均合理。

### 电池遥测通过，超声波场景仍需重摆

- 用户澄清两个开关后，按电池盒开关与上层板蓝色 POWER 开关的正确顺序重新准备，并回复“重新带电观察已准备好”。重新枚举 COM5 后执行 10 秒只读观察；没有发送 ARM、DRIVE 或 STOP。
- 会话收到 51 条遥测，全部保持 `mode=idle`、`state=idle`、左右 PWM 为 0；电池电压仅在 7448 与 7468 mV 两个值之间变化。由此确认当前电池供电和 A0 电压检测处于合理范围，并通过无负载电压门槛；尚未证明电机启动压降或长时间稳定性。
- 启动前两条测距为无回波，随后稳定在 213 至 217 mm，共 8 个离散值；现场准备声明的目标约为 500 mm，因此测距场景仍未通过。稳定窄范围更像探头前近处存在反射面，但在看到场地前不把这一推断写成确定根因。
- 本机原始记录为 `artifacts/local/robot-20260919T233142Z-d654c524.jsonl`。因为控制器 ARM 要求距离至少达到 300 mm，当前安全逻辑会拒绝运动；保持这一门槛，不为推进测试而降低阈值。
- 下一步断电后从两个金属探头正面水平量距，移除近处物体并把大平面目标放在约 600 mm 处；复测确认距离响应后，才执行一次 1 秒架空前进。

### 60 cm 目标复测达到启动门槛，但绝对精度待校准

- 用户按两个金属探头正面水平量距，将大平面目标重摆至标称 600 mm 并清除近处物体后回复“60 cm 目标已准备好”。沿用已确认的电池加 USB、蓝牙移除、四轮架空条件执行 5 秒只读观察。
- 收到 26 条遥测，全部保持 `mode=idle`、`state=idle`，非零 PWM 记录数为 0；电池为 7429 至 7468 mV，仍在正常窗口。测距为 344 至 371 mm，末值 363 mm，本机记录为 `artifacts/local/robot-20260919T233341Z-6e1ebb35.jsonl`。
- 与上一轮 213 至 217 mm 相比，读数随目标重摆明显改变并超过 300 mm ARM 门槛，足以进入受控架空方向测试；但相对声明的 600 mm 仍偏低约四成，不能把绝对测距标为通过。后续需用尺在多个已知距离复核是否仍有其他反射面、测量基准或安装角度问题，再决定是否调整换算。
- 下一步仅计划通过 COM5 发送左右各 70 PWM、持续 1 秒的前进动作，会话结束自动 STOP。执行前仍需用户明确确认支撑稳固、四轮及线缆完全无干涉并授权本次运动。

### 首次 1 秒前进请求因测距波动提前安全中止

- 用户明确回复“确认 COM5 执行 1 秒低速前进”，授权条件为四轮稳固架空、USB/衣物/手远离车轮、电池和两个开关开启、车头目标不动、可立即关闭 POWER。执行 `Robot.ps1 -Action drive -Port COM5 -Left 70 -Right 70 -Seconds 1 -ConfirmHardwareReady -ConfirmMotionClear`。
- 会话没有完成计划的 1 秒。初始遥测距离 277 mm、零 PWM；主机先发送 STOP 并获 ACK，随后 ARM MANUAL 获 ACK。第一次 `DRIVE 70 70` 获 ACK，紧随的一条遥测为 `mode=manual`、`state=manual`、距离 308 mm、左右 PWM 均为 70，证明非零命令至少短暂进入正式控制输出。
- 下一次 DRIVE 续发收到 ERR，主机立即发送 STOP 并获 ACK；末条遥测为 `mode=idle`、`state=stopped`、距离 286 mm、左右 PWM 均为 0。按固件时间戳，非零遥测到停止遥测相隔约 200 ms；这不是精确机械运行时长，也不能证明轮子实际旋转。
- 会话状态为 failed、`stop_acknowledged=true`，本机记录为 `artifacts/local/robot-20260919T233644Z-25245b67.jsonl`。安全中止表明测距在 300 mm ARM 边界附近波动；不自动重试、不降低障碍阈值。
- 在完整日志读取前曾即时说明“车轮没有收到非零 PWM”，随后已根据明确的 70/70 遥测向用户更正。等待用户报告是否看到短暂转动以及四轮方向；之后将把反射目标移得更远，再决定是否重做一次受控 1 秒动作。

### 将架空电机方向诊断与超声波安全门解耦

- 用户指出纸板已经放得很远，且架空确认轮子方向不应被测距测试阻塞。复盘确认：正式 `robot_car` 的前向安全门适合地面运行，但把它直接用于工作台电机极性确认，会让波动的超声波读数提前拒绝续发命令；上一次只有约 200 ms 的非零遥测，现场未能看清轮子。
- 新增 `firmware/motor_check/motor_check.ino`，仅用于四轮稳固架空的单轮诊断。每次启动先保持电机停止 3 秒，然后按正式固件相同的左右正向极性，以固定 PWM 80 同时驱动四轮 2 秒，随后 PWM 永久归零，直到再次复位。程序不附加舵机，也不读取超声波、循迹或电池 ADC。
- 在 `programs.json` 将其明确标为上电自动动作程序；仓库检查限制 PWM 不超过 90、动作不超过 2000 ms，并拒绝传感器/舵机调用。双语校准指南改为优先使用这一有界诊断，原厂会循环动作的 `motor_test` 仅保留作故障定位参考；来源和差异记录在 `THIRD_PARTY.md`。
- 该程序会临时覆盖正式固件，实测结束后必须重新上传 `robot_car`。本阶段仅完成源码与文档修改，尚未编译、上传或驱动电机；在再次确认电池供电断开、只接 COM5 USB、蓝牙移除及四轮架空前，不访问硬件。
- 离线验证随后完成：`Build.ps1 -Program motor_check` 为 1/1 通过，Flash 2480/32256 字节、静态 RAM 188/2048 字节，摘要为 `artifacts/local/compile-20260919T234450692Z.json`；`Check-Repository.ps1` 通过 70 个原厂文件哈希、固定 Unity 哈希、14 个程序入口和确认门槛；`Test-Docs.ps1` 通过 8 份指南与 48 个本地链接；`git diff --check` 无错误。这些结果不代表程序已上传或轮子已实测。
- 用户明确确认“已断开电池供电，确认 COM5 上传 motor_check”。上传前重新枚举到 COM5 为唯一标记 `Serial Port (USB)` 的端口；执行 `Upload.ps1 -Program motor_check -Port COM5 -ConfirmHardwareReady`，编译和上传退出码均为 0，Flash 2480/32256 字节、静态 RAM 188/2048 字节，摘要为 `artifacts/local/compile-20260919T234634289Z.json`。
- 当前板上最近一次成功上传的程序是 `motor_check`，已经临时覆盖 `robot_car`。上传期间电池供电保持断开，没有电机实物动作；下一步需拔 USB、接回电池并在四轮架空条件下观察一次 3 秒等待加 2 秒动作，随后关电并重新上传正式固件。
- 用户随后在 USB 与电池同时连接、四轮架空的条件下授权开始。通过受项目端口锁保护的有界 COM5 会话显式复位主控并采集 7 秒串口；收到完整的 `MOTOR_CHECK`、`RUN: left=80 right=80 for 2 seconds` 和 `DONE`，程序路径执行完毕并自动归零。
- 用户现场反馈“蜂鸣器响，电机不转”。由于该版本没有主动驱动 A0 蜂鸣器，声音来源尚未独立确认；低 PWM 下电机或驱动啸叫是一个可能解释，不记录为确定根因。原厂 `01.1.3_Car_Move_and_Turn` 实际使用 PWM 200，因此不再逐级试速，下一候选直接采用同一 PWM 200、仅持续 1 秒，并按原厂 A0 共用规则将蜂鸣器保持为输入且关闭上拉。
- 修订版离线验证通过：`Build.ps1 -Program motor_check` 为 1/1 成功，Flash 2560/32256 字节、静态 RAM 188/2048 字节，摘要为 `artifacts/local/compile-20260919T235449117Z.json`；仓库检查确认诊断上限为 PWM 200、1000 ms，14 个程序入口及原厂/Unity 哈希通过；双语文档检查和 `git diff --check` 通过。尚未上传该修订版，也未再次驱动电机。
- 用户确认两个开关关闭后，重新枚举 COM5 为唯一 USB 串口并上传修订版 `motor_check`；上传退出码为 0，Flash 2560/32256 字节、静态 RAM 188/2048 字节，摘要为 `artifacts/local/compile-20260919T235534001Z.json`。用户随后打开两个开关并确认四轮架空，第一次有界复位获得完整 PWM 200/1 秒串口步骤，现场反馈“成功转动”。
- 应用户要求，以相同程序、相同条件再执行一次 1 秒动作确认方向。串口再次完整到达 `DONE` 并自动归零；按“轮胎最上沿朝车头为正转”的统一判断标准，用户最终确认实际为反转。
- 因左右正向命令共同产生整车倒车，不按单轮故障处理。将正式固件的 `invert_left_motor`、`invert_right_motor` 同时改为 `true`，并把一次性诊断的方向脚从左 LOW/右 HIGH 改为左 HIGH/右 LOW。原厂快照保持不变，仓库检查新增正式固件与诊断极性一致性约束；修改后的正向动作仍待一次实车复测。
- 极性修正离线验证通过：78 项原生控制测试零失败；`motor_check` 编译为 Flash 2560/32256、RAM 188/2048，摘要 `artifacts/local/compile-20260920T011356346Z.json`；`robot_car` 编译为 Flash 14834/32256、RAM 668/2048，摘要 `artifacts/local/compile-20260920T011400607Z.json`。仓库检查、双语文档检查与 `git diff --check` 均通过；尚未上传修正版，也不能据此宣称物理方向已经纠正。
- 用户确认两个开关关闭后，上传极性修正版 `motor_check` 成功，摘要为 `artifacts/local/compile-20260920T012505602Z.json`。用户重新打开两个开关后执行同一 3 秒等待加 PWM 200/1 秒动作；串口完整到达 `DONE`，用户按统一观察标准反馈“似乎是正常的了”。因此将正向极性记为初步通过，不外推倒车、左右转向或地面轨迹已经通过。
- 准备恢复正式固件时复核固定原厂循迹源码：`03.2_Automatic_Tracking_Line` 明确以黑线读低电平工作，中心黑线对应原始 `101`，而项目控制器内部以黑线位为 1。将 `black_reads_high` 从 `true` 更正为 `false`，使硬件层先反相归一化；原厂快照保持不变。
- 架空 PWM 80 未能启动、PWM 200 已可靠启动。为避免首轮地面综合测试继续使用接近失速的 PWM 90，将正式巡航暂调为 150，保留上限 150 和转向减速 50；该值位于原厂使用范围内，但仍需实际路线验收后再降到可靠起步的最低值。当前板上仍是 `motor_check`，这些正式固件修改尚未重新验证或上传。
- 首轮验证中，78 项原生控制测试、正式固件编译、仓库与文档检查均通过；但 `Simulate.ps1` 按旧巡航 90/缓转 40 的 CSV 期望值报告 31 行中 7 行不一致。实际输出均对应新配置的直行 150、缓转 100，不是状态机异常。将 `scenarios/line_course.csv` 的这 7 个期望 PWM 同步到当前生产配置后重跑，不删除或放宽其他状态/故障断言。
- 同步期望后，路线回放 31/31 通过；78 项主机测试通过。再次确认 78 项原生控制测试、`robot_car` 编译（Flash 14836/32256、RAM 668/2048，摘要 `artifacts/local/compile-20260920T014722049Z.json`）、仓库检查、双语文档检查和 `git diff --check` 全部通过。当前板上仍是极性修正后的 `motor_check`；正式固件尚未刷回，地面循迹、障碍蜂鸣和恢复均未实测。
- 用户确认两个开关关闭后，重新枚举 COM5 为唯一 USB 串口并上传最终配置的 `robot_car`；编译与上传退出码为 0，Flash 14836/32256 字节、RAM 668/2048 字节，摘要为 `artifacts/local/compile-20260920T015053113Z.json`。当前板上最近一次成功上传的已恢复为正式固件，包含双侧极性反转、巡航 PWM 150、黑线低电平归一化、82 度舵机中点和障碍短鸣。
- 上传未发送 ARM 或运动命令，也不证明地面行为通过。下一步合并为一次无 USB 的地面综合验收：黑线循迹、前进方向/转弯、遇障停止和蜂鸣、移障后持续清空再恢复，最后红外停止。

## 2026-09-19 - 红外启动与地面起步复盘

- 首次地面综合测试中，用户按遥控器 `1` 时接收板指示灯闪烁，但没有观察到车轮动作。随后上传带诊断输出的正式固件；串口实测收到 `0xFF30CF`，`recognized=1`，但当时循迹输入为 `line=0`，启动被控制器以 `state=6`（`line_lost`）拒绝。协议类型实际为 `3`，因此先前移除协议分类限制属于兼容性加固，并不是这次拒绝的根因。
- 同一次带电只读观察中，电池遥测约为 7.35--7.37 V，超声波约为 340--370 mm。第二段 30 秒只读记录在遮挡中间循迹探头时得到 `line=2` 共 107/150 个采样，移动过程中短暂得到 `line=1`，移开后回到 `line=0`；因此三路输入链路至少能识别中心和单侧变化。记录分别保存在本机 `artifacts/local/robot-20260920T021713Z-11d43e8b.jsonl` 与 `artifacts/local/robot-20260920T021948Z-e10bbbee.jsonl`，不作为已发布制品。
- 用户随后将小车落地、中间探头置于黑色、两侧位于木色地板并再次按 `1`，仍未观察到动作；该次脱离 USB，无法从现场现象区分启动被拒与 PWM 不足。已知架空实测 PWM 80 不起转、PWM 200 能使四轮起转，而此前正式固件使用未经实车确认的 PWM 150。为消除该假设值，正式循迹和红外手动速度改为已验证的原厂基准 PWM 200，邻线缓转为 150；最大输出同步为 200。
- 主控板内置 `L` 灯改为仅在控制器实际处于已启动模式时常亮，停车或拒绝启动时熄灭，从而把“红外接收板看到信号”和“控制器接受启动”区分开。离线验证为控制测试 78/78、主机测试 78/78、场景回放 31/31；`robot_car` 编译使用 Flash 15012/32256 字节、静态 RAM 668/2048 字节，仓库、双语文档和差异检查均通过。此版本尚未上传或完成地面实测。
- 用户明确确认断开电池供电后，PWM 200 加状态灯版本成功上传到唯一 USB 串口 COM5，摘要为 `artifacts/local/compile-20260920T022736260Z.json`。随后脱离 USB 复测时，红外活动灯只随按键闪烁、状态灯没有保持、车轮没有动作且未进入循迹；因此该次失败发生在控制器启动门槛，而不是已知的 PWM 80 起转不足问题，提高 PWM 本身没有解决启动。
- 将红外 `1` 的语义改为持续进入循迹待命：有效输入存在时即使当前是障碍、脱线或歧义线型，也不在 ARM 阶段静默拒绝。运行中 `000` 脱线和 `101` 歧义均立即零 PWM 但保持模式，线路恢复后自动继续；`111` 作为交叉/全黑区域直行通过。明确的 `0`/Power 停止、低电压、无回波/过期/越界传感器故障和串口租约规则保持不变。
- 同步删除已经不用的 `line_lost_ms` 配置与旧文档声明，更新状态机测试和路线回放期望。最终离线验证再次通过控制测试 78/78、主机测试 78/78、路线回放 31/31、仓库与双语文档检查及 `git diff --check`；`robot_car` 编译为 Flash 14980/32256、RAM 661/2048，摘要 `artifacts/local/compile-20260920T023415175Z.json`。该持续待命版本尚未上传或实车验证。
- 用户明确授权上传持续待命版。重新确认 COM5 后上传成功，编译与上传退出码均为 0，Flash 14980/32256 字节、RAM 661/2048 字节，摘要为 `artifacts/local/compile-20260920T023651389Z.json`。当前板上最近一次成功上传的是持续待命版正式固件；上传过程没有发送启动或运动命令，地面循迹与障碍恢复仍待一次综合实车验收。
- 持续待命版地面复测中，用户报告状态灯常亮但车无动作。四轮架空后通过 COM5 进行一次完整诊断：红外 `1` 已使控制器进入 `mode=line`，但末态为 `state=line_lost`、`line=0`、距离 376 mm、电池 7311 mV、左右 PWM 0；原始记录为 `artifacts/local/robot-20260920T024127Z-e7af7069.jsonl`。这排除了红外启动、电池和障碍门槛，零输出来自循迹值。
- 复核 Freenove 官方 Chapter 3 后确认此前极性解释错误：官方明确规定黑色或无物体为 `1`、白色为 `0`，原厂 `000`/`010`/`101` 前进分支也与这一约定一致。当前 `black_reads_high = false` 会把标准的“浅色/黑线/浅色”原始 `010` 错误反相为 `101`，而项目把 `101` 作为歧义线型停车，因此能够完整解释“已启动但不走”。将配置恢复为 `black_reads_high = true`，同步更新仓库约束与当前文档；原厂快照保持不变。来源：<https://docs.freenove.com/projects/fnk0041/en/latest/fnk0041/codes/tutorial/3_Line_tracking.html>。该修正此刻尚未编译、上传或实车验证。
- 极性修正后的完整离线验证通过：原生控制测试 78/78、主机工具测试 78/78、路线回放 31/31；正式固件编译为 Flash 14976/32256 字节、RAM 661/2048 字节，摘要 `artifacts/local/compile-20260920T024436457Z.json`。仓库检查确认 70 个原厂文件哈希不变且 `black_reads_high = true` 约束生效，双语文档检查和 `git diff --check` 通过。当前板上仍是上一版错误反相固件，修正版尚未上传或实车验证。
- 用户关闭两个开关并断开电池后，Windows 重新枚举确认 `USB-SERIAL CH340 (COM5)`，极性修正版上传成功；Flash 14976/32256 字节、RAM 661/2048 字节，摘要为 `artifacts/local/compile-20260920T024800530Z.json`。上传未发送启动或电机指令，也尚未构成实车通过。
- 上电前安全复核发现与极性修正相关的第二处规则：官方的 `111` 同时可能表示全黑表面或三个探头均悬空。此前为通过交叉区域而让 `111` 直行，会在无地面反射时错误驱动。项目范围不要求交叉路口规划，因此将 `111` 改为零 PWM 的歧义等待，保持已启动状态并在重新取得 `001/010/011/100/110` 后自动恢复；`000` 仍作为全白脱线等待。此安全修正此刻尚未重新编译或上传。
- `111` 安全修正后的完整验证再次通过：原生控制 78/78、主机工具 78/78、路线回放 31/31，仓库、双语文档与差异检查均通过；正式固件编译为 Flash 14974/32256 字节、RAM 661/2048 字节，摘要 `artifacts/local/compile-20260920T024948416Z.json`。沿用用户已确认且未改变的电池断开、仅 USB、四轮架空状态，设备管理器确认 `USB-SERIAL CH340 (COM5)` 正常后重新上传成功，最终上传摘要为 `artifacts/local/compile-20260920T025009140Z.json`。当前板上最近一次成功上传的固件包含正确的黑白极性和 `111` 停车待命规则；尚待地面综合验收。

## 2026-09-20 - 串口循迹诊断启动时序修正

- 新视频确认用户实际按下红外数字 `1`，车轮没有动作；视频中的短促声音无法单独确定来源。为排除遥控链路，用户明确确认四轮架空、线路卡片固定、USB 与电池供电就绪并授权 COM5 运行 10 秒串口循迹诊断。
- 首次 `Robot.ps1 -Action line` 没有驱动电机。主机在收到启动后 200 ms 的首帧遥测后发送 STOP 并获 ACK，随后发送 `ARM LINE`，主控立即返回 ERR，主机再次 STOP 并获 ACK。该帧为 `line=2`、`range_mm=0`、`battery_mv=7311`、左右 PWM 0；原始记录为 `artifacts/local/robot-20260920T135211Z-95fd944d.jsonl`。
- 紧接的 10 秒只读观察收到 51 帧，循迹值始终为 2，电池约 7.33 V，超声波读数已恢复且末值为 331 mm；原始记录为 `artifacts/local/robot-20260920T135300Z-a6528348.jsonl`。因此主控、循迹、电池和超声波并非持续故障；前一次拒绝来自串口打开复位后，主机在 400 ms 舵机稳定期和首次有效测距之前过早 ARM。
- `host/robot_tool.py` 现会在安全 STOP 后等待最多 3 秒，直到项目遥测出现 `20..4000 mm` 的有效测距才发送运动 ARM；超时则保持停止、明确报告 `no ARM sent`，不自动重试。JSONL 新增 `motion_ready` 事件，记录等待时间和启动前距离。
- 主机测试新增“启动初期无回波后恢复才 ARM”和“持续无回波绝不 ARM”两项，主机套件由 78 项增至 80 项并全部通过；`git diff --check` 通过。该修复只改电脑端工具，不需要重新上传 Arduino 固件；修复后的实车串口循迹仍待保持相同安全布置后复测。
- 用户确认原安全布置未变后，修正主机时序的第二次 COM5 循迹会话成功收到 `ARM LINE` 的 ACK，但第一次 PING 被拒并自动 STOP。原始记录 `artifacts/local/robot-20260920T140010Z-236c3013.jsonl` 显示 ARM 前 `line=2`、距离 324 mm、电池 7331 mV；约 200 ms 后状态锁定为 `low_battery`，PWM 已归零，而停车后的电压读数又恢复为 7331 mV。该顺序证明电机启动后至少出现过一次低于 6.6 V 的采样，遥控器不是本次停止原因；视频中的短促声音也与启动后瞬停相符，但仅凭现有记录仍不能把声音源确定为蜂鸣器。
- 独立 `motor_check` 已在相同电池路径下以 PWM 200 完成 1 秒四轮动作，而正式控制器原先对任意单次低压采样立即永久停车。为保留电池保护并过滤四电机启动浪涌/ADC 瞬态，ARM 前低于 6.6 V 仍立即拒绝；运行中改为连续低于阈值 300 ms 才锁定 `low_battery`，恢复到阈值以上会清除待确认计时。没有降低 6.6 V 门槛。
- 新增配置边界、ARM 低压拒绝、瞬时低压继续、持续低压停车和恢复后重新计时测试。最终离线结果为原生控制 82/82、主机 80/80、场景回放 35/35；`robot_car` 编译为 Flash 14972/32256 字节、RAM 668/2048 字节，摘要 `artifacts/local/compile-20260920T140255273Z.json`；仓库、双语文档和 `git diff --check` 全部通过。修正版尚未上传，当前板上仍是单采样低压锁停的上一版正式固件。
- 用户确认关闭电池盒开关和上层蓝色 POWER、拔下圆形电池插头、只保留 USB、蓝牙移除且四轮架空后，Windows 确认唯一健康 CH340 为 COM5。执行 `Upload.ps1 -Program robot_car -Port COM5 -ConfirmHardwareReady` 成功，Flash 14972/32256 字节、RAM 668/2048 字节，摘要 `artifacts/local/compile-20260920T140503122Z.json`。当前板上最近一次成功上传的是持续低压确认版正式固件；上传阶段没有发送 ARM 或运动命令，实车结果仍待带电串口验收。
- 用户接回电池并恢复同一架空线路布置后，修正版 10 秒串口循迹会话完整结束并收到最终 STOP ACK，原始记录为 `artifacts/local/robot-20260920T140617Z-3f732470.jsonl`。电压为 7.214--7.331 V，距离为 318--641 mm，`low_battery=0`、`sensor_fault=0`，证明单次启动低压不再错误锁停。但 25 帧遥测中 19 帧为 `line=0`、4 帧为 `line=7`，仅启动前为 `line=2`，全程没有采到非零 PWM；因此低压修复通过软件/遥测验收，循迹运动仍未通过。
- 在不发送任何运动命令、现场布置未再次调整的情况下紧接着采集 10 秒对照，51/51 帧均稳定为 `line=4`、电压 7.292--7.311 V、距离 416--437 mm，记录为 `artifacts/local/robot-20260920T140708Z-fe8664bd.jsonl`。这排除持续电气噪声导致三路输入随机跳变；当前线路卡片与探头的相对位置已经落在单侧探头，而非期望的中心 `line=2`。下一步只需在静止只读状态下重新对准并确认 `line=2`，不再修改代码或上传。

## 2026-09-20 - PWM 150 正向短动与地面验收准备

- 用户重新对准线路卡片后，只读记录 `artifacts/local/robot-20260920T141219Z-9670ee4d.jsonl` 的 16/16 帧均为中心线型 `line=2`；电池为 7.272--7.311 V，测距为 300--306 mm，左右 PWM 均为 0。这只确认静止输入布置，不代表循迹运动通过。
- 用户确认四轮架空、线缆与手部远离、电池和 USB 供电正常后，授权 COM5 执行左右 PWM 150、计划 2 秒的正式固件手动短动。控制器接受 ARM 和前六次 DRIVE，四帧遥测均为 `mode=manual`、左右 PWM 150；随后第七次 DRIVE 返回 ERR，主机立即发送 STOP 并收到 ACK。非零输出约持续 0.75 秒，原始记录为 `artifacts/local/robot-20260920T141302Z-b6fd3320.jsonl`，因此不能写成完整 2 秒会话通过。
- 该短动的遥测在启动后出现 5.180 V、6.099 V 的低值，随后恢复到 7.018 V 和 6.920 V；距离从 330/316/473 mm 变化到 249 mm。现有 ERR 帧没有携带控制器停止原因，不能仅凭这份记录在低压、下一次测距越过 200 mm 门槛或其他输入故障之间作确定归因，也没有观察到主控重启。
- 用户现场确认四轮方向正常且可以起转。由此只将 PWM 150 的正向起转记为通过，不外推持续供电稳定性、地面牵引、转弯、循迹或障碍恢复。
- 将正式巡航和输出上限从 PWM 200 降至 150，邻线缓转为 100；同步生产场景期望值和双语校准说明。下一步只进行一次短距离地面综合验收：红外 `1` 启动，中心循迹和宽缓弯修正，20 cm 内遇障停车并短鸣，移到 30 cm 外持续确认后恢复，脱线/歧义线型零输出待命，最后用 `0` 锁止。手机语音控制仍是后续扩展，尚未实现或实测。
- 降速后的离线验证通过：原生控制测试 82/82、主机工具测试 80/80、生产逻辑场景回放 35/35，仓库与双语文档检查及 `git diff --check` 均通过。正式固件编译为 Flash 14972/32256 字节、RAM 668/2048 字节，摘要为 `artifacts/local/compile-20260920T141811652Z.json`。当前主控仍运行上一次上传的 PWM 200 正式固件；只有在断开电池供电后重新上传，PWM 150 才会在实车上生效。
- 用户明确确认“已断开电池，可以上传 PWM 150 正式固件”。上传前重新枚举到 COM5 为唯一标记 `Serial Port (USB)` 的端口；执行 `Upload.ps1 -Program robot_car -Port COM5 -ConfirmHardwareReady` 后编译和上传均退出成功，Flash 14972/32256 字节、RAM 668/2048 字节，编译摘要为 `artifacts/local/compile-20260920T141940622Z.json`。当前主控最近一次成功上传的程序已更新为 PWM 150 正式固件；上传过程未发送 ARM 或运动命令，地面综合功能仍待现场验收。
- 用户随后报告综合功能已经产生预期动作，但在木色地板上无法可靠识别黑线。由于未逐项回报循迹、停车、蜂鸣、恢复和 `0` 停车结果，本记录不把整套综合验收标为全部通过；明确保留“木地板循迹未通过”这一现场结果。
- Freenove FNK0041 官方 Chapter 3 的指定基准是白色平面上的黑线，并提醒阳光中的红外会干扰反射传感器。木色、木纹和表面高光可能使底色与黑胶带落在相同数字阈值；当前车端只接收三路二值输入，若两种表面产生相同位值，状态机无法从软件端重建丢失的反射对比度。
- 首选处理不是再次改控制器，而是使用哑光白色海报纸/泡沫板作底，中央贴哑光黑线；必须留在木地板时，先铺足够覆盖三路探头的白色带状底面再贴黑线。标准静止验收值为全白 `000`、中黑两侧白 `010`、左黑 `100`、右黑 `001`；仍失败时再单独评估模拟量读取或更换可校准传感器，不把未经确认的板上元件当作灵敏度旋钮盲调。
- 用户进一步确认当前小车核心功能测试已经通过，并决定最终使用白色区域，不再要求木色地板兼容。该结论按用户现场观察记录；最终项目仍需在固定白底黑线场地完成重复路线和视频/数据留证。此前约定的手机语音控制尚未实现，因此“核心小车功能通过”不等同于全部项目范围和课程交付已经完成。

## 2026-09-20 - 安卓 BLE 语音控制首版

### 目标与设计边界

- 用户开始布置最终白底黑线场地，同时授权开发语音控制。首版选择 Android，因为现有 BT05 为 BLE 串口透明通道，原厂资料明确支持 Android/iPhone、默认名称 `BT05`、115200 8N1，且连接后作为数据管道；没有为手机另刷原厂蓝牙车固件。
- App 复用已经上传和验证过的项目协议：`STOP`、`ARM LINE`、`ARM MANUAL`、`DRIVE`、`PING`、`STATUS`。车端障碍、低压、传感器故障、PWM 上限和租约仍是最终安全边界，手机不能绕过。
- 有限中英文词表只映射停止、循迹和四个方向。停止候选优先；语音方向固定运行 1 秒并以 100 ms 刷新，循迹以 500 ms 心跳续租；未知词不动作，手动按钮按住运行、松手停止，App 后台/断连停止续租并发送 STOP。

### 实现内容

- 新增 `mobile/android` 原生 Java App：BLE 扫描、记忆上次设备、GATT 服务发现、优先 `FFE1` 并回退到首个可写特征、通知/遥测接收、20 字节兼容分片、串行写队列、系统 `SpeechRecognizer`、中英文资源、方向/循迹/停止控制和遥测面板。
- Android 12 及以上使用 `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`，旧系统扫描声明粗略/精确定位，麦克风独立请求。App 不存录音，并禁用对上次蓝牙地址的云备份和设备迁移。
- 新增协议、词表、遥测解析 6 项 JUnit 测试；新增固定工具链、构建、显式设备确认后安装脚本。APK、SDK、Gradle 缓存和调试签名均被 Git 忽略。
- 固定 Gradle 8.10.2、Android Gradle Plugin 8.8.2、Command-line Tools 12.0、SDK/API 35、Build Tools 35.0.0、JUnit 4.13.2；Gradle 和命令行工具下载均校验 SHA-256。

### 构建问题与修复

- 初次脚本从仓库根调用 Gradle，因设置文件位于 `mobile/android` 而失败；改为显式工程目录。
- Android 插件和 Windows 测试进程不能可靠处理课程归档中的中文路径：源码编译成功但测试类在运行时报告 `ClassNotFoundException`。没有跳过测试；构建脚本改为把源文件复制到 `%LOCALAPPDATA%` 下经过边界检查的 ASCII 临时工程，再执行测试、Lint 和打包。
- 首轮 Lint 报 3 个错误和 16 个警告，逐项修复旧系统定位权限、不可翻译应用名、启用蓝牙权限证明、备份规则、文本资源和 UI 告警；未建立 baseline 或关闭检查。最终 Lint 文本为 `No issues found.`。
- APK 额外检查发现平台内置图标引用未打包；替换为项目内资源，并把 `aapt` 包名、最低/目标 SDK 和三项关键权限校验加入每次构建。

### 验证结果与剩余工作

- `Build-Android.ps1` 完整通过 48 个 Gradle task；6/6 单元测试零失败，Android Lint 零问题，Debug APK 大小 44,758 字节，SHA-256 `8fe3c500c683cacd8d08eb6fe59e9b1bef13181353adfc6e2dde40adec7babfb`，本机路径为被忽略的 `artifacts/local/SEP780-Robot-Car-debug.apk`。
- APK 清单实际解析为包名 `io.github.shengqinchu.sep780`、最低 API 26、目标 API 35，并含扫描、连接和录音权限。仓库检查通过 70 个原厂文件哈希、安卓控制边界和安装确认门槛；文档检查通过 10 份指南、61 个本地链接；`git diff --check` 通过。
- ADB 只读枚举为空，随后已关闭临时启动的 ADB server；没有手机被访问或安装。没有打开 COM5、没有修改/上传 Arduino 固件、没有驱动车轮。
- 当前证据仅到“实现、单元测试、Lint、APK 构建与清单校验”。仍需用户连接安卓手机后安装，在四轮架空条件下连接本车 BT05，依次验证 STOP、四向限时动作、遥测/ACK，再到白底黑线场地验收语音循迹、障碍停车蜂鸣和恢复。

## 2026-09-20 - PWM 100 默认、App 调速与手机接入准备

- 用户在白底黑线场地布置期间反馈 PWM 150 仍偏快，先要求改为 100，并把 Redmi Note 11 Pro 通过 USB 接到电脑。第一版将车端、安卓端和主机工具硬锁为 100；此前 PWM 80 架空未起转、150 已起转，因此 100 仍是待验证试验值，不能从编译结果推断地面可靠起步。
- 用户随后提出从手机 App 调速。最终设计改为：固件上电默认 `cruise_pwm=100`，`max_pwm=150`；App 用滑杆提供 100–150、步长 10 的六档选择。新增 `S <seq> SPEED <pwm>`，仅在停车状态、串口/BLE 来源且数值处于范围内时接受；拖动滑杆先 STOP，每次手动或循迹 ARM 前再次下发所选速度。障碍、低压、传感器故障、租约与 150 硬上限保持车端强制。
- 新增默认范围、合法改速、越界、红外来源拒绝、运行中拒绝和协议解析/执行测试。最终离线结果为原生控制 86/86、主机工具 80/80、生产场景回放 35/35；`robot_car` 编译为 Flash 15128/32256 字节、RAM 675/2048 字节，摘要为 `artifacts/local/compile-20260920T152124900Z.json`。当前小车仍运行上一版 PWM 150 固件，本轮没有访问 COM 口、上传固件或驱动车轮。
- 首次重建固定 PWM 100 Android 包时，遥测样例已改为左 100/右 50，但右轮断言仍为 100，6 项测试中 1 项失败；没有跳过测试。修正后中间 APK 通过；加入速度滑杆与 `SPEED` 编码后的最终 48-task 构建再次通过，6/6 单元测试零失败、Lint 零问题，APK SHA-256 为 `0a755ad03d4efb5b459e90dcd2752e9e3bb015e4dca8f18753b77b3af138a602`。
- Windows 即插即用只读枚举确认目标手机为 `Redmi Note 11 Pro`，MTP 状态正常，说明数据线可传输；`adb devices -l` 仍为空，因此 USB 调试/RSA 授权尚未建立。没有向手机安装 App，也没有访问蓝牙或小车。下一步先在手机上启用 USB 调试并接受本机 RSA 提示，再安装 APK；另需小车断电、移除蓝牙模块并只接主控 USB 后，才可上传支持默认 100/动态调速的固件。
- 用户完成 Redmi 的 USB 调试授权后，ADB 唯一识别到型号 `21091116C`、Android 13。首次安装被 HyperOS 的 `INSTALL_FAILED_USER_RESTRICTED` 拒绝；确认普通未知来源已允许后，打开开发者选项并由用户启用“USB 安装”，同一 APK 随后返回 `Success`。包名 `io.github.shengqinchu.sep780`、版本 `0.1.0`，冷启动成功、进程存活且 AndroidRuntime 崩溃日志为空。
- 按用户要求增加 App 内“中文 / English”切换。Android 13 使用系统 `LocaleManager`，Android 8–12 使用本地配置回退；两种路径都保存选择，语音识别 locale 随界面语言变化。若已连车，切换会先 STOP、断开 BLE，再刷新 Activity；两套命令词仍同时保留。
- 首次英文实机截图发现三等分连接区的 `DISCONNECT` 折行；缩减连接按钮水平留白并关闭全大写后复测为单行。最终 Android 构建 48 个 task 全部通过，9/9 单元测试零失败、Lint `No issues found.`，APK 50,876 字节，SHA-256 `c76d52ec8971dabcc5ddee12d403fd80c3b9237997a10cd82e534c34d51f8f7d`。
- 使用 UI 层级中的 `language_zh` / `language_en` 控件完成红米实机验收：中文切英文成功，英文切中文成功，强制结束并冷启动后仍保持中文；中英文截图均无文本重叠，最终手机停留在中文未连接主页。证据保存在被忽略的 `artifacts/local/sep780-ui-*-redmi.png`。本轮没有请求附近设备或麦克风权限，没有扫描/连接 BT05、访问 COM5、上传固件或驱动车轮；小车仍是上一版 PWM 150 固件。
- 收尾回归通过仓库约束、10 份双语指南/61 个本地链接、Android 9/9、原生控制 86/86、主机 80/80、路线回放 35/35、pySerial 内存回环和 Arduino 33/33 全量编译；最终编译摘要为 `artifacts/local/compile-20260920T155808946Z.json`。这些是软件与无硬件回归证据，不替代 BT05 和实车验收。

## 2026-09-20 - BLE 实机扫描与设备识别增强

- 用户将蓝牙模块插回上层扩展板后要求开始连接测试。本轮边界限定为 BLE 扫描、设备识别和后续状态握手；没有点击方向、循迹或语音控制，也没有访问 COM5、上传固件或驱动车轮。小车当前仍运行上一版 PWM 150 固件，因此动态 `SPEED` 命令尚不能进行实车验收。
- Redmi Note 11 Pro 的蓝牙已开启，目标 App 位于前台；首次点击扫描触发并获得 Android 13 的“附近设备”授权，`BLUETOOTH_SCAN` 和 `BLUETOOTH_CONNECT` 均为 granted，麦克风权限仍未请求。首次 8 秒扫描成功返回多台 BLE 设备，证明手机扫描链路可用，但原界面仅调用 `BluetoothDevice.getName()`，把绝大多数设备显示为“未命名”，无法安全判断哪一台属于小车。
- 将扫描项改为优先读取 `ScanRecord.getDeviceName()`，同时显示地址和 RSSI，并按实时 RSSI 从强到弱排列。已有地址会刷新设备、名称和信号值，避免用第一次出现的陈旧强度排序；连接仍只在用户选择具体条目后发生，扫描本身不发送机器人协议命令。
- 诊断版重新执行 `Build-Android.ps1`，48 个 Gradle task 全部成功，9/9 单元测试零失败，Lint 零问题；APK SHA-256 为 `f6071103d1b6bfadcf3db6d8596a5d757f748c42933c5526d87dca30961618ea`。使用已确认的 ADB 设备序列覆盖安装成功，原有附近设备授权得到保留。
- 第二次实机扫描能够按 RSSI 显示广播名称和信号；截图保存在 `artifacts/local/sep780-ble-scan-rssi-redmi.png`。列表中没有出现官方默认名 `BT05`，也没有出现可直接确认的同类名称，因此没有随机连接任何匿名设备。当前结论只到“App 与手机 BLE 扫描通过、目标模块尚未识别”；下一步先由用户确认模块指示灯和整车供电状态，再在保持无运动命令的情况下复扫或用通断电差分识别地址。

## 2026-09-20 - BLE 双向链路与安全停止实机验收

- 通过保持手机位置不变并分别关闭、开启小车蓝牙供电的差分扫描，唯一锁定目标模块，近距离 RSSI 约为 -48 dBm。该识别依据来自实际通断电变化，不再依赖原厂默认名 `BT05`；公开日志不记录设备广播名和硬件地址。
- Redmi 点击该精确地址后 App 显示已连接，并持续收到正式固件遥测：循迹位、超声波距离、电池电压和左右 PWM 均能更新。首次示例为距离约 522--534 mm、电池 8.35--8.37 V、左右 PWM 0，证明手机到模块、模块到主控以及主控通知回手机的双向链路均已建立。
- 连接时模块可能向透明串口注入自身状态文本，旧版车端将其记为 `command_error`。在四轮架空且未发送任何运动指令的前提下，App 发送一次安全 `STOP`；随后实机回传 `mode=idle`、`state=stopped`、`line=7`、`range=1234 mm`、`battery=8.35 V`、左右 PWM 0。验收截图保存为 `artifacts/local/sep780-ble-stop-redmi.png`。
- 手机在连接状态下旋转屏幕时，Android 重建 `MainActivity` 并关闭 GATT，日志确认这不是 STOP 导致的断连。控制器是单屏竖向操作面板，因此将该 Activity 锁定为竖屏，避免操作中误旋转丢失连接；主动切换中英文仍会按设计先 STOP、断开再刷新界面。
- 本轮只验收连接、遥测和 STOP，没有点击方向、循迹、语音或速度控制。小车当前仍运行上一版默认 PWM 150 且不支持 `SPEED` 的正式固件；仓库中的默认 PWM 100 和手机 100--150 调速只有重新完成断电上传后才能做硬件验收。
- 竖屏修复后的 Android 构建 48 个 task 全部成功，9/9 单元测试与 Lint 通过；APK SHA-256 为 `c0dbaf59c52b791f1463387c32d5e682a5d6cbb79d0320c2c46dbc9cdcd31fb3`。合并及打包清单均确认 `MainActivity` 为 `screenOrientation="portrait"`，随后覆盖安装到已确认的 Redmi 成功，冷启动进程存活且没有 AndroidRuntime 崩溃。仓库检查、10 份双语指南/61 个链接和 `git diff --check` 也全部通过；覆盖安装没有连接小车或发送机器人命令。
- 用户按完整上传准备要求确认断开电池供电并连接 USB 后，即插即用层重新确认唯一目标为 `USB-SERIAL CH340 (COM5)`；COM8--COM13 均为 Windows 蓝牙虚拟串口，没有误用。上传前核对待刷固件为默认 `cruise_pwm=100`、`max_pwm=150` 且包含 `SPEED` 解析与车端边界。
- 执行受保护的 `Upload.ps1 -Program robot_car -Port COM5 -ConfirmHardwareReady` 后编译 1/1 与上传均成功；Flash 15,128/32,256 字节、静态 RAM 675/2,048 字节，摘要为 `artifacts/local/compile-20260920T175500624Z.json`。当前主控最近一次成功上传的固件已经支持默认 PWM 100 和停车状态下的 100--150 动态调速；上传阶段没有发送 ARM、DRIVE 或其他运动命令，手机调速、方向和语音仍待四轮架空实测。

## 2026-09-20 - BLE 调速与四向架空输出验收

- 用户确认恢复电池和蓝牙模块、拔除主控 USB 且四轮架空后，Redmi App 自动恢复到已连接状态。测试前先发送 STOP，主控回传 `mode=idle`、`state=stopped`、左右 PWM 0、电池 8.23 V，证明新固件启动后仍处于安全停止态。
- 在 App 选择 PWM 100 时依次按住四向按钮约 0.6--1.1 秒，每次释放均自动 STOP。运行中遥测分别为：前进 `100/100`（8.11 V）、左转 `-100/100`（8.09 V）、右转 `100/-100`（8.05 V）、后退 `-100/-100`（7.94 V）；模式均为 `manual`，未出现低压或传感器故障状态。对应截图保存在 `artifacts/local/sep780-pwm100-*-active.png`。
- 将速度滑杆从 100 拖到 150 时，App 显示“已选择速度，运动保持停止”，主控仍回传 `idle/stopped` 和 `0/0`，证明改速本身不会驱动车轮。随后一次短前进的运行中回传为 `150/150`、电池 7.74 V，证明 BLE `SPEED` 协议与 150 上限在实机上生效；证据为 `artifacts/local/sep780-pwm150-selected.png` 和 `sep780-pwm150-forward-active.png`。
- 测试后将滑杆恢复 PWM 100 并再次 STOP，最终回传为 `mode=idle`、`state=stopped`、左右 PWM 0、电池 8.21 V，证据为 `artifacts/local/sep780-final-pwm100-stopped.png`。蓝牙连接全程保持，屏幕方向修复没有再次触发 GATT 重建。
- 上述截图证明手机命令、固件解析、状态机和左右电机输出值正确，但小车没有轮速编码器，遥测不能独立证明轮子肉眼所见的实际旋转与方向；该物理观察仍需用户现场确认后才能记为通过。语音识别和落地 PWM 100 循迹也不在本轮架空输出验收范围内。

## 2026-09-20 - App 最低 PWM 110、单页圆形遥控与语音状态复核

### 目标与设计结果

- 用户现场确认 PWM 100 偶发出现单侧电机不起转，因此只把安卓 App 的可选范围提高为 110、120、130、140、150；车端协议和上电默认继续保留 100，避免破坏红外、主机工具和已有固件边界。110 仍需在地面负载下重复确认。
- 按用户指定重构为单页遥控：顶部为中英文切换和明确的蓝牙连接区，连接状态灯在就绪时使用蓝色；电池、测距和三路循迹作为三个同级指标；中央 270 度圆弧吸附到五档 PWM，四向按钮环绕圆盘；语音、循迹在下方，停止键固定于底部。布局不含滚动容器。
- 圆弧开始触摸时先调用停止，松手才提交合法档位。方向按钮继续使用按住运行、松手停止；App 进入后台、断连或销毁时取消续租并停止。
- 设计参考了 Android 官方示例、Home Assistant、SpeedView、openHAB Android 和 Material Components 的信息层级与设备状态表达；没有复制其代码、图片、图标或布局，也没有加入新依赖。比较及许可边界记录在 `research/android-dashboard-ui-2026-09-20.md`。

### 语音功能与证据边界

- Android 系统 `SpeechRecognizer` 已接入 App，界面语言同时决定 `zh-CN` 或英文识别 locale。首次使用单独申请麦克风权限，App 本身不保存录音。
- 有限词表支持停止/停车、开始循迹/循迹、前进、后退/倒车、左转和右转及其英文对应词。多个识别候选中停止优先；未知词不发送运动；四个语音方向动作固定为 1 秒，循迹使用 500 ms 心跳续租。
- 词表映射和停止优先已通过单元测试，语音按钮、解析器、安全控制器和 BLE 协议在代码中连通。手机当前已有录音权限，但本轮没有连接小车、没有启动语音识别会话，也没有在真车上说出命令，因此状态只能写为“已接入，实车未验收”。

### 问题、修复与验证

- 第一轮单页截图发现英文 `Disconnect` 宽度不足；重新分配三个连接按钮宽度并把连接按钮字号限定为 11 sp，中英文最终截图均保持单行且无重叠。
- 第一轮最终构建的 Lint 为 0 error / 30 warning，主要来自旧 UI 遗留资源、小字号和固定竖屏提示。删除未使用资源、修正字号/对称间距和布局属性；固定竖屏用于避免操作中旋转重建 Activity 并关闭 GATT，只对该已知取舍和必要的单页嵌套权重做局部抑制，没有建立全局 baseline。
- 最终 `Build-Android.ps1` 完成 48 个 Gradle task；11/11 单元测试零失败，Lint 为 `No issues found.`。APK 大小 63,679 字节，SHA-256 为 `3117b40120dfb233c18ccf0b9231ee9c9dbe1bb2d3bc814b23cb2d615d32bbea`。
- 最终 APK 已覆盖安装到 Redmi Note 11 Pro / Android 13。中英文单页截图分别为 `artifacts/local/ui-round-remote-final-zh.png` 和 `artifacts/local/ui-round-remote-final-en.png`；层级检查确认无可滚动节点、英文 `Disconnect` 完整、冷启动默认 PWM 110。断开 BLE 时圆弧触控完成 110→150→110，左右输出始终为 0；手机最终停在中文、未连接、PWM 110。
- 仓库约束、10 份双语指南/61 个本地链接和 `git diff --check` 在收尾后重新执行。整个 UI 验收没有连接 BT05、没有访问 COM5、没有上传固件或发送运动命令。剩余实物门槛为 PWM 110 地面负载重复起转、语音四向/停止/循迹以及障碍联锁验收。

## 2026-09-20 - 双语项目演示稿与现场执行方案

- 复核本地 2026 Fall 课程资料：`Project Presentation and Report` 占总评 35%；学习计划将 `Demo and Report` 安排在 2026 年 12 月 3 日所在周。现有资料没有给出演示时长、最终报告篇幅或更细评分 rubric，因此没有把建议的三分钟稿写成官方时限，并明确最终以 Avenue 公告为准。
- 新增 `docs/demo-guide.md` 与 `docs/demo-guide.en.md`。两份指南使用同一流程，包含白底黑线场地布局、10 分钟前检查、角色分工、约三分钟中英文逐句稿、五分钟扩展模块、常见提问、故障恢复、录像证据和五次运行记录表。
- 演示范围与固件参数对齐：20 cm 障碍停车、30 cm 恢复阈值、600 ms 连续确认、通过通断电差分确认本车蓝牙模块、PWM 110 候选下限，以及“遥测 PWM 不是实测轮速”的证据边界。切换语言会停止并断开连接，因此指南要求连接前选好语言，不在运行中切换；公开日志不记录唯一设备名。
- 当前只完成演示材料，不把项目写成已经最终验收。正式 `Ready` 仍要求 PWM 110 地面负载可靠起步、语音真车控制，以及同一固定路线至少五次留证；本轮没有访问 COM5、上传固件、连接蓝牙或驱动车轮。

## 2026-09-20 - 录像版中英对照短句稿

- 用户明确最终交付是录像 demo，并要求中英文逐句对照、短句和美国高中生水平语法。新增 `docs/demo-script-bilingual.md` 作为唯一逐字稿，按时间逐段对应画面、操作、中文和英文；原演示手册不再保留另一套长句稿，统一链接到该文件。
- 新稿增加 App 屏幕介绍：App 由项目组自行开发，使用原生 Java、Android SDK、BLE GATT 和 Android 系统 `SpeechRecognizer`；逐项解释语言选择、蓝色连接状态、电池/距离/循迹指标、110--150 PWM 圆弧、四向控制、语音、循迹和固定停止按钮。
- 录像方案采用一条从上电前到最终停止的连续全景作为主证据，App 屏幕录制或第二机位只作画中画；实际“前进 / Forward”“开始循迹 / Follow line”“停止 / Stop”保留现场原声。指南明确主录像只选一种语言，避免连接中切换语言触发安全停止和 BLE 断开。
- 五次固定路线尚未完成前，稿件提供替换句，避免提前声称已有重复实车结果。本轮仅修改文档与文档测试，没有连接小车、访问 COM5、上传固件或发送运动命令。

## 2026-09-20 - 地面弯道惯性与最低起步 PWM 冲突

- 用户在实际路线发现：能够可靠起步的速度会因惯性错过弯道，尚未完成修正便冲出黑线；继续降低 PWM 又可能无法克服电机静摩擦而起步。这是“起步扭矩”和“巡航速度”共用一个参数造成的控制边界，不记录为单纯场地误差。
- 当前生产逻辑在中心 `010` 时连续输出 `cruise/cruise`；轻微偏移 `110/011` 时输出 `cruise-50` 与 `cruise`；单侧 `100/001` 时才让内侧轮归零。以 App 最低 110 为例，直线为 `110/110`、早期修正约为 `60/110`，只能在看到偏移后改变转弯半径，不能独立解决起步冲量和持续车速。
- 推荐下一版采用非阻塞混合控制，而不是长期大 PWM 的机械式全停/全启：从真正静止进入运动时先给约 60--80 ms 的有界启动脉冲；随后使用较低占空比的短驱动/短滑行循环，并在 `110/011` 早期偏移时立即让内侧轮停转、外侧轮低速脉冲。初始候选为启动 `PWM 150`、运行 `PWM 110`、约 60 ms 驱动加 40 ms 滑行；这些只是首轮参数，尚未实车验证。
- 控制器必须在驱动和滑行窗口内持续读取线路、测距、电压、BLE 租约和停止命令，不能使用 `delay()`；脱线、歧义线型、障碍和显式 STOP 仍立即输出零。40 ms 滑行不应被当作每次重新起步，只有较长时间真正归零后才允许再次触发启动脉冲。首版不使用反转制动，避免齿轮冲击和额外电流尖峰。
- 本轮只完成代码复核和方案选择，没有修改固件、访问 COM5、上传程序或驱动车轮。后续应先做一次包含连续巡航、宽缓弯、急弯、脱线和障碍的综合 A/B 测试，再决定最终脉冲时序。

## 2026-09-20 - 开放 PWM 50–150 实验范围

- 用户澄清此前没有完成系统性的低速逐档测试，要求先把 PWM 下限开放到 50。此次不把先前 PWM 80/100 的零散观察当作可靠速度门槛，也没有直接实现启停脉冲控制；先用同一版软件测量真实起转范围。
- 正式固件的 `min_runtime_pwm` 从 100 改为 50，上电 `cruise_pwm=100` 和硬上限 150 不变。Android 与 Python 协议同步改为 50–150、步长 10；App 冷启动默认仍为 110。圆形仪表盘由固定 5 个刻度改为按协议动态显示 11 个刻度。
- 新增/更新边界测试：车端默认配置接受 50、拒绝 49；Android 和 Python 接受 50，拒绝 40、55 和 151。C++ 控制器 87/87、Python 主机 80/80、Android 11/11、Lint、Arduino 编译及 35/35 场景回放全部通过。`robot_car` 编译占 Flash 15,128/32,256 字节、静态 RAM 675/2,048 字节。
- Debug APK 大小 63,683 字节，SHA-256 为 `3b8b0dac9a6e15f81e219f502bb39b45a3f566cb84930ea49c02550049479959`。ADB 只发现一台已授权 Redmi Note 11 Pro，覆盖安装成功；包版本仍为 `0.1.0`，更新时间为 2026-09-20 16:03:05。本轮没有启动 App、连接 BLE 或发送运动指令；公开日志不记录 ADB 序列号。
- 完成软件构建时，新版固件尚未上传到主控，因此当时的真车仍不会接受低于旧下限的 `SPEED`。50–100 只是实验档位；协议应答或遥测输出不等于电机实际起转。后续上传结果及只读验收记录如下，下一步仍需在四轮架空状态从 50 每次加 10 短测，最后落地重复测试并选出正式演示的最低可靠档位。
- 用户随后明确确认“电池与蓝牙已断开、只接 USB、四轮架空、可以上传 COM5”。受保护上传重新编译 1/1 通过，`avrdude` 完成写入与校验；摘要为 `artifacts/local/compile-20260920T200746056Z.json`。从此时起，主控最近一次成功上传的正式固件已接受 50–150、步长 10 的停车调速命令。
- 上传后执行 3 秒只读串口验收，没有发送 STOP、SPEED、ARM 或 DRIVE。共收到 16 条正式遥测，末条为 `mode=idle`、`state=idle`、左右 PWM `0/0`、距离 439 mm；断开电池时电压通道读到约 0.527 V。原始记录为 `artifacts/local/robot-20260920T200823Z-e8336e9d.jsonl`。这证明固件已启动并保持零输出，但仍不构成低速电机动作通过。

## 2026-09-20 - 循迹专用 PWM 120 非阻塞脉冲

- 用户完成 50–100 的实体逐档观察并报告这些档位均不能可靠起步，因此不再用 App 低档直接控制循迹。最终实现边界为：手动和语音方向继续采用 App 选择的连续 PWM；循迹独立固定为 PWM 120，驱动 80 ms、零 PWM 滑行 80 ms。
- `Controller` 使用基于 `millis()` 的相位计算，没有加入 `delay()`。每次 tick 仍先检查输入新鲜度、超声波、低压保持、串口租约和线路状态；STOP、丢线、歧义、障碍、低压或传感器故障均立即输出 `0/0` 并重置脉冲。线路重新有效时从新的驱动相位起步。直行为 `120/120`，缓转内轮为 70，急转内轮为 0。
- Android `startLine()` 不再发送圆弧所选 `SPEED`；圆弧仅影响手动按钮和一秒语音方向动作。车端仍保留 50–150 的手动实验范围，避免把“协议接受”误写成“电机一定起转”。
- 新增循迹脉冲交替、恢复后重启驱动相位、手动不脉冲、计时回绕和非法脉冲配置测试。`Test-Control.ps1` 为 92/92，`Test-Host.ps1` 为 80/80，生产场景回放为 35/35；`robot_car` 编译占 Flash 15,426/32,256 字节、RAM 685/2,048 字节，摘要为 `artifacts/local/compile-20260920T202021891Z.json`。
- Android 48 个 Gradle task 全部成功，11/11 单元测试通过，Lint 通过；APK SHA-256 为 `f4ea133a36bf02674fc11a2336dfdeaf80e3bc4328684a7973fa9bce702d28ec`。ADB 只识别到一台已授权的 Redmi Note 11 Pro，覆盖安装成功；安装没有连接 BLE 或发送运动命令，公开日志不记录 ADB 序列号。
- 用户明确确认蓝牙与电池供电断开、只接 USB 并授权刷写。即插即用层确认目标为 `USB-SERIAL CH340 (COM5)`；执行受保护上传后编译与写入均成功，摘要为 `artifacts/local/compile-20260920T202305308Z.json`。上传没有发送 ARM 或 DRIVE。
- 上传后 3 秒只读观察收到 16 条正式遥测；末条为 `mode=idle`、`state=idle`、左右 PWM `0/0`、距离 401 mm，原始记录为 `artifacts/local/robot-20260920T202330Z-30991959.jsonl`。这证明脉冲版已经启动并默认停止，不证明电机脉冲、弯道、障碍停车或恢复已经通过实体路线验收。
- 首轮白底黑线实测由用户现场确认直线运行正常，但进入弯道后修正幅度不足，最终丢失中线并按设计停车。该结果证明直线脉冲可用，同时暴露原 `turn_reduction_pwm=50` 让侧偏时内轮仍以 70 前进，不能把问题只归因于路线。
- 保持 PWM 120 和 80/80 ms 周期不变，只将 `turn_reduction_pwm` 从 50 改为 120，使 `110/011` 与 `100/001` 一样在驱动相位使用内轮 0、外轮 120。没有加入脱线后盲转或反向搜索，`000/101/111` 及所有障碍/故障停车规则不变。
- 增加生产配置左右侧偏均为 `0/120` 或 `120/0` 的回归测试；离线结果为控制测试 93/93、场景回放 35/35，正式固件编译为 Flash 15,426/32,256 字节、RAM 685/2,048 字节，摘要为 `artifacts/local/compile-20260920T204328306Z.json`。
- 用户再次明确确认电池圆形供电和蓝牙模块均已拔下、只接 USB。`USB-SERIAL CH340 (COM5)` 身份确认后，转向增强版编译上传成功，摘要为 `artifacts/local/compile-20260920T204545769Z.json`；上传未发送 ARM 或 DRIVE。
- 上传后 3 秒只读观察收到 16 条正式遥测，末态 `mode=idle`、`state=idle`、左右 PWM `0/0`、距离 459 mm；原始记录为 `artifacts/local/robot-20260920T204607Z-572e80cc.jsonl`。这证明新版刷写和默认停止，弯道改善仍必须在同一路线复测。

## 2026-09-21 - App 可选步进 / 连续循迹与弯道扭矩 A/B

- 转向增强步进版在白底黑线弯道再次停住。现场照片显示车头已经偏离切线，但底部至少有一个侧向循迹指示灯仍亮；用户同时听到与 80/80 ms 周期相近的重复声音。该证据说明并非已知的“全部脱线后静默等待”，但照片拍摄方向不能单独确认规范化后的左右位，声音来源也不能只凭照片确定。结合当前侧偏命令为单侧 `0/120`、每次只通电 80 ms，主要待验证假设是外侧单轮脉冲无法从静止克服弯道负载，而不是直接继续猜传感器坏了。
- 新增停车态串口命令 `LINE PULSE` 和 `LINE CONTINUOUS`。两档均固定使用 `line_pulse_pwm = 120`，直行 `120/120`，侧偏内轮 0、外轮 120；Pulse 保留 80 ms 驱动 / 80 ms 零输出，Continuous 只取消零输出阶段。两档继续逐循环执行 STOP、串口租约、`000/101/111`、障碍、低压和传感器故障检查，未加入丢线盲转或自动搜索。
- 循迹档只允许串口/BLE 在未启动状态设置，运行中切换会被拒绝。App 切换档位或开始循迹时先发送 STOP，再发送所选档位，最后才 `ARM LINE`；红外 `1` 每次都恢复 Pulse，避免上一次手机选择悄悄改变独立遥控行为。
- 安卓单页界面在状态行加入“步进 / 连续”分段控制，中英文可切换。手动圆弧仍只控制 50--150 的手动/语音方向速度，不覆盖两种循迹档。切换档位会终止当前调度并保持停车。
- 新增默认档、停车态权限、连续保持输出、连续档丢线/障碍归零、红外恢复步进、两条新协议命令与非法 token 测试。原生控制测试为 99/99，主机测试为 80/80，生产场景回放为 35/35。Android 最终重建的 48 个 Gradle task 成功，11/11 JVM 测试及 Lint 通过；Debug APK SHA-256 为 `49f5ff3c71115a66df2df714d6019eca4aee03f5be1ff0ccbeefd5c7518138a1`。
- 仓库检查、13 份指南的 75 个本地链接、中英 README 命令一致性和 `git diff --check` 均通过。新增静态断言首次错误地寻找连续源码文本 `LINE PULSE`，而解析器实际按两个 token 比较；断言改为核对 `CommandType::LinePulse` / `LineContinuous` 后通过，没有改动运行逻辑。
- `robot_car` 离线编译使用 Flash 15,604/32,256 字节、静态 RAM 704/2,048 字节，摘要为 `artifacts/local/compile-20260921T202602960Z.json`。完成离线检查后，用户明确确认电池与蓝牙断开、只接小车 USB、四轮架空，红米已连接电脑。ADB 唯一确认一台已授权 Redmi；双档 APK 覆盖安装成功，包名 `io.github.shengqinchu.sep780`、版本 `0.1.0`，手机记录更新时间为 2026-09-21 16:34:53。公开日志不记录 ADB 序列号。
- 即插即用枚举再次确认目标小车为 `USB-SERIAL CH340 (COM5)`。执行受保护上传后编译与写入成功，Flash 15,604/32,256 字节、RAM 704/2,048 字节，新摘要为 `artifacts/local/compile-20260921T203504345Z.json`。上传期间没有发送 ARM 或 DRIVE，电池和蓝牙均保持断开。
- 上传后只读观察 3 秒，共收到 16 条正式遥测；末条为 `mode=idle`、`state=idle`、左右 PWM `0/0`、距离 385 mm，原始记录为 `artifacts/local/robot-20260921T203528Z-792ad9f0.jsonl`。这证明新版固件启动并保持零输出，不证明弯道已修复。下一项仍是在相同起点依次复测 Pulse 和 Continuous；只有 Continuous 能在侧向位持续存在时转动，才确认脉冲扭矩/占空比是主要原因。

## 2026-09-21 - 正常混合循迹与急弯主动转向

- 用户在相同场地完成实体 A/B 并报告：步进档仍卡在原急弯，全连续档则直接冲出并丢失黑线。该结果说明单外轮 80 ms 脉冲不足，而把直线也改为连续又带来过高速度；两种极端都不适合作为最终演示档。此处是用户现场观察，不把它写成独立仪器测量。
- 复核原厂固定快照后确认 `A1/A2/A3` 分别为左/中/右，现有 `100/110` 左修正和 `001/011` 右修正映射与原厂一致。用户提出的“内轮慢、外轮快”会比当前内轮停转的转弯半径更大，因此未采用；也没有按场地坐标或固定时间写死转弯。
- 新增协议档 `LINE HYBRID`，App 第二档从“连续 / Continuous”改为“正常 / Normal”。正常档在 `010` 时继续 PWM 120 的 80 ms 驱动 / 80 ms 滑行；`110/011` 时连续使用内轮 0、外轮 120；`100/001` 时连续使用 `-120/120` 或 `120/-120` 原地修正。旧 `LINE CONTINUOUS` 保留为诊断命令，但不再由 App 选择。
- `000/101/111`、障碍、STOP、低压、通信租约和传感器故障仍立即输出 `0/0`；本轮没有加入丢线后的盲目搜索。方向反转继续经过 I/O 层的零 PWM 过渡周期，避免直接换向。
- 新增正常档中线脉冲、相邻位连续、单侧位左右反向和立即停车联锁测试。原生控制 103/103、主机 80/80、生产场景回放 35/35、仓库静态约束均通过。`robot_car` 编译为 Flash 15,712/32,256 字节、RAM 710/2,048 字节，摘要为 `artifacts/local/compile-20260921T205223726Z.json`。
- Android 48 个 Gradle task 成功，11/11 JVM 测试和 Lint 通过；新 APK SHA-256 为 `62be5802e0721a8dd98739ae5e697108b8d99897a7905465e66ed5dc6b358798`。本轮新混合版尚未安装到 Redmi、尚未刷入 COM5，也没有通过新逻辑驱动车轮。下一步必须先四轮架空确认 `100` 与 `001` 的物理转向方向，再落地复测同一弯道。
- 用户随后明确确认电池与蓝牙断开、只接小车 USB、四轮架空，红米已连接电脑。只读枚举再次确认小车唯一 USB 串口为 COM5，ADB 唯一授权设备仍为 Redmi `21091116C`。新 APK 覆盖安装成功，手机记录更新时间为 2026-09-21 17:07:07；安装没有连接 BLE 或发送运动命令，公开日志不记录 ADB 序列号。
- 受保护上传重新编译并成功写入混合固件；Flash 15,712/32,256 字节、RAM 710/2,048 字节，摘要为 `artifacts/local/compile-20260921T210720101Z.json`。上传期间没有发送 ARM 或 DRIVE。随后 3 秒只读观察收到 16 条遥测，末态为 `idle`、左右 PWM `0/0`、距离 553 mm；原始记录为 `artifacts/local/robot-20260921T210744Z-7b3f7b7f.jsonl`。该证据只确认安装、刷写和默认零输出，架空转向与地面弯道仍待验收。

## 2026-09-21 - 正常档转弯专用 PWM 150

- 用户在四轮架空、车头朝向操作者的三位置测试中报告：中间循迹位使四轮前进；一个侧位只看到一侧前后轮前进；另一个侧位从静止无明显动作，但先保持中间位前进再立即切换时，原先不动作的一侧可以转动。车头朝向造成视觉左右镜像，因此不单凭“操作者左/右”给轮组命名；可以确定的是，静止起转和已有运动后的响应不同。
- 复核主循环和 `Fnk0041Io::wheel()`：控制输出每个循环都会重写，正反向切换只插入一个零 PWM 保护周期，下一循环会继续施加目标方向，因此没有发现“反向命令只写一次后丢失”的软件路径。现场现象更符合反向冷启动扭矩不足，但尚未用电流、电压或转速仪器证明，不能写成电机或驱动板损坏。
- 固定的原厂快照提供了同方向证据：原厂自动循迹直行为约 PWM 90，转弯档达到 `160/-140`；左右单轮示例也分别以 100 和 255 演示两个方向。本车此前 PWM 150 的双向架空电机测试已通过。因此没有提高直线速度或开放超过现有上限，而是在生产配置新增 `line_turn_pwm = 150`。
- 正常档 `010` 继续使用 PWM 120 的 80 ms 驱动 / 80 ms 滑行；`110/011` 改为内轮 0、外轮连续 150；`100/001` 改为连续 `-150/150` 或 `150/-150`。步进档、手动/语音 50--150 范围、障碍与脱线停车、低压和通信租约规则均未改变。
- 更新配置边界、正常档左右输出及仓库静态约束测试。原生控制 103/103、生产场景回放 35/35、仓库检查全部通过。`robot_car` 离线编译为 Flash 15,724/32,256 字节、RAM 711/2,048 字节，摘要为 `artifacts/local/compile-20260921T213318758Z.json`。
- 本轮没有访问或刷写 COM5，没有连接 BLE，也没有发送运动命令。PWM 150 转弯版处于“已实现、已测试、已编译”，尚未“已刷入、硬件实测”；当前主控仍运行上一版 PWM 120 转弯固件。下一步在用户重新确认电池和蓝牙断开、只接 USB、四轮架空后刷写，再用一次左/中/右三位置综合测试验收。
- 用户随后明确确认电池圆形供电与蓝牙模块均已拔下、只接下层 USB-C、四轮架空，并授权 COM5 上传。即插即用信息再次确认 `USB-SERIAL CH340 (COM5)`、VID/PID `1A86:7523`、状态正常。
- 受保护上传重新编译并成功写入；Flash 15,724/32,256 字节、RAM 711/2,048 字节，摘要为 `artifacts/local/compile-20260921T214326706Z.json`。上传期间没有电机供电，也没有发送 ARM 或 DRIVE。
- 随后执行 3 秒只读串口观察，共收到 16 条遥测；末态为 `idle`、左右 PWM `0/0`、距离 297 mm、电池通道 566 mV，原始记录为 `artifacts/local/robot-20260921T214400Z-abbe24a4.jsonl`。这确认 PWM 150 转弯版已经启动且默认零输出，但不证明带电转弯动作通过。下一步仍为三位置架空硬件验收。

## 2026-09-21 - 原子赛车遥控、最新值 BLE 队列与手动鸣笛

- 用户报告正常循迹档比步进档成功率明显提高，随后要求强化手动遥控：去除频繁 `command rejected; stop`，允许更高的手动输出，改为赛车式按压/摇杆 UI，并新增手动蜂鸣器按钮。该现场描述只表示相对改善，没有伪造固定路线成功率。
- 复核旧 Android 调度发现，每次手动开始都会按固定 120 ms 延时依次发送 STOP、SPEED、ARM 和 DRIVE；连续操作时旧帧可能与新操作交错。固件手动遇障又会直接退出模式，下一帧 DRIVE 因模式已丢失而被拒绝。这两条路径共同解释了已观察的拒绝现象，比单纯提高 PWM 更直接。
- 调研并记录 OpenBot、DonkeyCar、controlwear Virtual Joystick Android 和 BradenBoucher/rc-car。采用的是通用设计思想：油门/转向差速混控、输入死区、响应曲线、渐进加减速、反向前零输出、松手归零、固定发送周期与只保留最新值；没有复制源码或新增这些项目为依赖。完整比较见 `research/rc-controller-2026-09-21.md` 和 `THIRD_PARTY.md`。
- 车端新增紧凑 `R left right` 命令。非零帧原子完成串口接管、进入手动模式和更新两轮输出，`R 0 0` 幂等停止运动；最大序号和 `-200/-200` 仍恰好落在传统 BLE UART 的 20 字节单包边界。手动上限提高到 200，但循迹继续固定为中线 PWM 120 脉冲和 PWM 150 转弯。
- Android 改为 20 Hz 赛车摇杆，速度上限 110--200、默认 150。输入通过 8% 油门死区、10% 转向死区、非线性响应、差速混控、归一化、每帧最多 +15 加速和 -30 减速；从静止的非零主轮请求抬到已实测可起步的 110，方向反转先发送零。BLE 写队列保留正在发送的 GATT 分片，只替换尚未发送的旧摇杆帧；首次接管和松手停止使用优先帧。
- 手动障碍规则改为只阻止左右请求和为正的净前向移动，并保留会话；倒车和原地转向可用于脱离。低压、无效/过期测距、输入过期、350 ms 运动租约、1200 ms 串口租约和 STOP 继续有效，没有按“遥控不受限制”移除安全联锁。
- App 新增长按“鸣笛 / Hold horn”按钮和 `H 1` / `H 0` 协议。按钮按住时每 1 秒续租，车端 1.5 秒未续租自动停止；STOP 也立即静音。FNK0041 板载器件为有源蜂鸣器，A0 与电池 ADC 共用，因此采用 120 ms 响 / 30 ms 静音窗口并只在静音阶段采样。它可做重复短响或节奏提示，不能可靠改变音高演奏旋律；没有把“唱歌”写成已支持功能。
- 单页 UI 移除旧圆弧仪表和四向按钮，只保留蓝色连接状态、三项遥测、步进/正常选择、110--200 上限滑杆、大型模拟摇杆、语音、鸣笛、循迹和固定 STOP。中英文字符串、校准、协议、演示稿、测试计划与 README 同步更新。
- 离线结果：C++ 控制器 114/114，Python 主机 80/80，Android 16/16、48 个 Gradle task 和 Lint 全部通过，生产场景回放 35/35，仓库约束与 70 个原厂文件哈希通过，13 份指南/75 个本地链接通过，`git diff --check` 通过。全量 Arduino 编译 33/33；`robot_car` 使用 Flash 16,452/32,256 字节和 RAM 727/2,048 字节，摘要为 `artifacts/local/compile-20260921T232146219Z.json`。
- 新 Debug APK 为 `artifacts/local/SEP780-Robot-Car-debug.apk`，SHA-256 `4f74015a72a8a77ef0558a355461363c8c431ef41e9e63c0614c2fee21861c7a`。离线验证完成时尚未覆盖安装到 Redmi 或刷入 COM5；后续装机证据记录如下，不能用后续结果改写这一阶段边界。
- 用户随后确认按要求连接并授权 COM5 刷写与手机安装。只读枚举确认小车为 `USB-SERIAL CH340 (COM5)`、VID/PID `1A86:7523`，ADB 只发现一台已授权 Redmi `21091116C`；公开日志不记录 ADB 序列号。执行受保护上传成功，Flash 16,452/32,256 字节、RAM 727/2,048 字节，摘要为 `artifacts/local/compile-20260921T233344846Z.json`；上传期间没有发送运动或鸣笛命令。
- `scripts/Install-Android.ps1` 返回 `Success`。已安装包为 `io.github.shengqinchu.sep780`、版本 `0.1.0`、手机记录更新时间 2026-09-21 19:34:07。分别启动中英文单页并检查完整布局和进程存活，未见 AndroidRuntime 崩溃；截图为 `artifacts/local/sep780-remote-installed.png` 与 `artifacts/local/sep780-remote-installed-zh.png`。检查期间 BLE 保持断开，未发送车控命令。
- COM5 刷写后执行 3 秒只读观察，不发送 ARM、DRIVE、SPEED、鸣笛或 STOP。共收到 15 条正式遥测，末条为 `mode=idle`、`state=idle`、左右 PWM `0/0`、距离 935 mm、电池通道 547 mV；原始记录为 `artifacts/local/robot-20260921T233611Z-12c31f59.jsonl`。电池当时断开，因此低电压读数不代表电池状态。该证据确认本轮固件已启动并保持默认零输出，但 PWM 200、摇杆手感、松手/反向、障碍下倒车或原地转向、手动鸣笛、STOP 和 BLE 断连仍需一次带电架空综合验收。
- 用户随后自行完成带电综合操作并反馈“目前是好用的”，因此把本轮手机/小车集成从“仅装机”更新为“现场总体可用”。该反馈没有逐项次数、视频或串口日志，不据此宣称每一个验收动作都已独立重复通过。
- 同一轮测试仍偶发小车不响应，App 显示 `command rejected`。失败时没有同步保存 ACK、控制器状态或遥测，当前不能区分安全联锁拒绝、BLE 帧时序、租约过期或其他原因；不继续猜测，也不在本轮修改已装机代码。该问题暂不阻塞当前 demo 准备，正式录像彩排若复现则保留现场状态与日志，之后再做定向修复。
